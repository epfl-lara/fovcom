/*
Code automatically generated by asn1scc tool
*/
package asn1src

import asn1scala._
import stainless.lang._
import stainless.annotation._
import stainless.collection._
import stainless.proof._
import StaticChecks._

/*-- TTC_6_20_DataArea --------------------------------------------*/

case class TTC_6_20_DataArea (
    startAddress: TPUSC_UINT32, 
    length: TPUSC_UINT32
) {

    def size(offset: Long): Long = {
        require((0L <= offset) && (offset <= 9223372036854775743L))
        val size_0 = 32L
        val size_1 = 32L
        size_0 + size_1
    }.ensuring { (res: Long) => 
        res == 64L
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaAnyOffset(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854775743L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854775743L))
        val size_0_offset = 32L
        val size_0_otherOffset = 32L
        val size_1_offset = 32L
        val size_1_otherOffset = 32L
        ()
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextByte(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854775743L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854775743L))
        require((offset % 8L) == (otherOffset % 8L))
        val size_0_offset = 32L
        val size_0_otherOffset = 32L
        val size_1_offset = 32L
        val size_1_otherOffset = 32L
        ()
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextWord(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854775743L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854775743L))
        require((offset % 16L) == (otherOffset % 16L))
        val size_0_offset = 32L
        val size_0_otherOffset = 32L
        val size_1_offset = 32L
        val size_1_otherOffset = 32L
        ()
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextDWord(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854775743L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854775743L))
        require((offset % 32L) == (otherOffset % 32L))
        val size_0_offset = 32L
        val size_0_otherOffset = 32L
        val size_1_offset = 32L
        val size_1_otherOffset = 32L
        ()
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }
}


@inline @cCode.inline val ERR_TC_6_20_DATAAREA: Int = 849 /* */
@inline @cCode.inline val ERR_TC_6_20_DATAAREA_STARTADDRESS_2_2: Int = 834 /* */
@inline @cCode.inline val ERR_TC_6_20_DATAAREA_LENGTH_2: Int = 844 /* */

 

@inline @cCode.inline val ERR_ACN_ENCODE_TC_6_20_DATAAREA: Int = 852 /*  */
@inline @cCode.inline val ERR_ACN_ENCODE_TC_6_20_DATAAREA_STARTADDRESS_2_2: Int = 837 /*  */
@inline @cCode.inline val ERR_ACN_ENCODE_TC_6_20_DATAAREA_LENGTH_2: Int = 847 /*  */
@inline @cCode.inline val TTC_6_20_DataArea_REQUIRED_BYTES_FOR_ACN_ENCODING = 8
@inline @cCode.inline val TTC_6_20_DataArea_REQUIRED_BITS_FOR_ACN_ENCODING = 64

@inline @cCode.inline val ERR_ACN_DECODE_TC_6_20_DATAAREA: Int = 853 /*  */
@inline @cCode.inline val ERR_ACN_DECODE_TC_6_20_DATAAREA_STARTADDRESS_2_2: Int = 838 /*  */
@inline @cCode.inline val ERR_ACN_DECODE_TC_6_20_DATAAREA_LENGTH_2: Int = 848 /*  */
/*-- TTC_6_20_DumpRawMemoryDataAreasToFile --------------------------------------------*/


case class TTC_6_20_DumpRawMemoryDataAreasToFile_dataAreas (nCount: Int, arr: Vector[TTC_6_20_DataArea])
{
    require(this.arr.size <= 63 && 1 <= this.nCount && this.nCount <= this.arr.size)

    def size(offset: Long): Long = {
        require((0L <= offset) && (offset <= 9223372036854771775L))
        TTC_6_20_DumpRawMemoryDataAreasToFile_dataAreas.sizeRange(this.arr, offset, 0, this.nCount)
    }.ensuring { (res: Long) => 
        (0L <= res) && (res <= 4032L)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaAnyOffset(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854771775L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854771775L))
        TTC_6_20_DumpRawMemoryDataAreasToFile_dataAreas.sizeLemmaAnyOffset(this.arr, offset, otherOffset, 0, this.nCount)
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextByte(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854771775L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854771775L))
        require((offset % 8L) == (otherOffset % 8L))
        TTC_6_20_DumpRawMemoryDataAreasToFile_dataAreas.sizeLemmaNextByte(this.arr, offset, otherOffset, 0, this.nCount)
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextWord(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854771775L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854771775L))
        require((offset % 16L) == (otherOffset % 16L))
        TTC_6_20_DumpRawMemoryDataAreasToFile_dataAreas.sizeLemmaNextWord(this.arr, offset, otherOffset, 0, this.nCount)
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextDWord(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854771775L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854771775L))
        require((offset % 32L) == (otherOffset % 32L))
        TTC_6_20_DumpRawMemoryDataAreasToFile_dataAreas.sizeLemmaNextDWord(this.arr, offset, otherOffset, 0, this.nCount)
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }
}
object TTC_6_20_DumpRawMemoryDataAreasToFile_dataAreas {
    def sizeRange(ls: Vector[TTC_6_20_DataArea], offset: Long, from: Int, to: Int): Long = {
        require((0 <= from) && (from <= to) && (to <= ls.size) && (ls.size <= 63))
        require((0L <= offset) && (offset <= 9223372036854775807L - 64L * (to - from)))
        decreases(to - from)
        if (from == to) {
            0L
        } else {
            val elemSize = ls.apply(from).size(offset)
            assert(elemSize == 64L)
            elemSize + TTC_6_20_DumpRawMemoryDataAreasToFile_dataAreas.sizeRange(ls, offset + elemSize, from + 1, to)
        }
    }.ensuring { (res: Long) => 
        val nbElems = (to - from)
        (0L <= res && res <= 64L * nbElems)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaAnyOffset(ls: Vector[TTC_6_20_DataArea], offset: Long, otherOffset: Long, from: Int, to: Int): Unit = {
        require((0 <= from) && (from <= to) && (to <= ls.size) && (ls.size <= 63))
        require((0L <= offset) && (offset <= 9223372036854775807L - 64L * (to - from)))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854775807L - 64L * (to - from)))
        decreases(to - from)
        if (from == to) {
            ()
        } else {
            val elemSizeOff = ls.apply(from).size(offset)
            val elemSizeOtherOff = ls.apply(from).size(otherOffset)
            assert(elemSizeOff == 64L)
            assert(elemSizeOtherOff == 64L)
            ls.apply(from).sizeLemmaAnyOffset(offset, otherOffset)
            TTC_6_20_DumpRawMemoryDataAreasToFile_dataAreas.sizeLemmaAnyOffset(ls, offset + elemSizeOff, otherOffset + elemSizeOtherOff, from + 1, to)
        }
    }.ensuring { (_: Unit) => 
        TTC_6_20_DumpRawMemoryDataAreasToFile_dataAreas.sizeRange(ls, offset, from, to) == TTC_6_20_DumpRawMemoryDataAreasToFile_dataAreas.sizeRange(ls, otherOffset, from, to)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextByte(ls: Vector[TTC_6_20_DataArea], offset: Long, otherOffset: Long, from: Int, to: Int): Unit = {
        require((0 <= from) && (from <= to) && (to <= ls.size) && (ls.size <= 63))
        require((0L <= offset) && (offset <= 9223372036854775807L - 64L * (to - from)))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854775807L - 64L * (to - from)))
        require((offset % 8L) == (otherOffset % 8L))
        decreases(to - from)
        if (from == to) {
            ()
        } else {
            val elemSizeOff = ls.apply(from).size(offset)
            val elemSizeOtherOff = ls.apply(from).size(otherOffset)
            assert(elemSizeOff == 64L)
            assert(elemSizeOtherOff == 64L)
            ls.apply(from).sizeLemmaNextByte(offset, otherOffset)
            TTC_6_20_DumpRawMemoryDataAreasToFile_dataAreas.sizeLemmaNextByte(ls, offset + elemSizeOff, otherOffset + elemSizeOtherOff, from + 1, to)
        }
    }.ensuring { (_: Unit) => 
        TTC_6_20_DumpRawMemoryDataAreasToFile_dataAreas.sizeRange(ls, offset, from, to) == TTC_6_20_DumpRawMemoryDataAreasToFile_dataAreas.sizeRange(ls, otherOffset, from, to)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextWord(ls: Vector[TTC_6_20_DataArea], offset: Long, otherOffset: Long, from: Int, to: Int): Unit = {
        require((0 <= from) && (from <= to) && (to <= ls.size) && (ls.size <= 63))
        require((0L <= offset) && (offset <= 9223372036854775807L - 64L * (to - from)))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854775807L - 64L * (to - from)))
        require((offset % 16L) == (otherOffset % 16L))
        decreases(to - from)
        if (from == to) {
            ()
        } else {
            val elemSizeOff = ls.apply(from).size(offset)
            val elemSizeOtherOff = ls.apply(from).size(otherOffset)
            assert(elemSizeOff == 64L)
            assert(elemSizeOtherOff == 64L)
            ls.apply(from).sizeLemmaNextWord(offset, otherOffset)
            TTC_6_20_DumpRawMemoryDataAreasToFile_dataAreas.sizeLemmaNextWord(ls, offset + elemSizeOff, otherOffset + elemSizeOtherOff, from + 1, to)
        }
    }.ensuring { (_: Unit) => 
        TTC_6_20_DumpRawMemoryDataAreasToFile_dataAreas.sizeRange(ls, offset, from, to) == TTC_6_20_DumpRawMemoryDataAreasToFile_dataAreas.sizeRange(ls, otherOffset, from, to)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextDWord(ls: Vector[TTC_6_20_DataArea], offset: Long, otherOffset: Long, from: Int, to: Int): Unit = {
        require((0 <= from) && (from <= to) && (to <= ls.size) && (ls.size <= 63))
        require((0L <= offset) && (offset <= 9223372036854775807L - 64L * (to - from)))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854775807L - 64L * (to - from)))
        require((offset % 32L) == (otherOffset % 32L))
        decreases(to - from)
        if (from == to) {
            ()
        } else {
            val elemSizeOff = ls.apply(from).size(offset)
            val elemSizeOtherOff = ls.apply(from).size(otherOffset)
            assert(elemSizeOff == 64L)
            assert(elemSizeOtherOff == 64L)
            ls.apply(from).sizeLemmaNextDWord(offset, otherOffset)
            TTC_6_20_DumpRawMemoryDataAreasToFile_dataAreas.sizeLemmaNextDWord(ls, offset + elemSizeOff, otherOffset + elemSizeOtherOff, from + 1, to)
        }
    }.ensuring { (_: Unit) => 
        TTC_6_20_DumpRawMemoryDataAreasToFile_dataAreas.sizeRange(ls, offset, from, to) == TTC_6_20_DumpRawMemoryDataAreasToFile_dataAreas.sizeRange(ls, otherOffset, from, to)
    }
}

case class TTC_6_20_DumpRawMemoryDataAreasToFile (
    memory_ID: OptionMut[TMemory_ID], 
    filePath: TFilePath, 
    dataAreas: TTC_6_20_DumpRawMemoryDataAreasToFile_dataAreas
) {
    require(this.memory_ID.isDefined)

    def size(offset: Long): Long = {
        require((0L <= offset) && (offset <= 9223372036854714357L))
        val size_0 = (this.memory_ID match {
            case SomeMut(v) =>
                32L
            case NoneMut() =>
                0L
        })
        val size_1 = this.filePath.size(offset + size_0)
        val size_2 = 32L
        val size_3 = this.dataAreas.size(offset + size_0 + size_1 + size_2)
        size_0 + size_1 + size_2 + size_3
    }.ensuring { (res: Long) => 
        (0L <= res) && (res <= 61450L)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaAnyOffset(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854714357L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854714357L))
        val size_0_offset = (this.memory_ID match {
            case SomeMut(v) =>
                32L
            case NoneMut() =>
                0L
        })
        val size_0_otherOffset = this.memory_ID match {
            case SomeMut(v) =>
                32L
            case NoneMut() =>
                0L
        }
        val size_1_offset = this.filePath.size(offset + size_0_offset)
        val size_1_otherOffset = this.filePath.size(otherOffset + size_0_otherOffset)
        this.filePath.sizeLemmaAnyOffset(offset + size_0_offset, otherOffset + size_0_otherOffset)
        val size_2_offset = 32L
        val size_2_otherOffset = 32L
        val size_3_offset = this.dataAreas.size(offset + size_0_offset + size_1_offset + size_2_offset)
        val size_3_otherOffset = this.dataAreas.size(otherOffset + size_0_otherOffset + size_1_otherOffset + size_2_otherOffset)
        this.dataAreas.sizeLemmaAnyOffset(offset + size_0_offset + size_1_offset + size_2_offset, otherOffset + size_0_otherOffset + size_1_otherOffset + size_2_otherOffset)
        ()
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextByte(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854714357L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854714357L))
        require((offset % 8L) == (otherOffset % 8L))
        val size_0_offset = (this.memory_ID match {
            case SomeMut(v) =>
                32L
            case NoneMut() =>
                0L
        })
        val size_0_otherOffset = this.memory_ID match {
            case SomeMut(v) =>
                32L
            case NoneMut() =>
                0L
        }
        val size_1_offset = this.filePath.size(offset + size_0_offset)
        val size_1_otherOffset = this.filePath.size(otherOffset + size_0_otherOffset)
        this.filePath.sizeLemmaNextByte(offset + size_0_offset, otherOffset + size_0_otherOffset)
        val size_2_offset = 32L
        val size_2_otherOffset = 32L
        val size_3_offset = this.dataAreas.size(offset + size_0_offset + size_1_offset + size_2_offset)
        val size_3_otherOffset = this.dataAreas.size(otherOffset + size_0_otherOffset + size_1_otherOffset + size_2_otherOffset)
        this.dataAreas.sizeLemmaNextByte(offset + size_0_offset + size_1_offset + size_2_offset, otherOffset + size_0_otherOffset + size_1_otherOffset + size_2_otherOffset)
        ()
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextWord(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854714357L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854714357L))
        require((offset % 16L) == (otherOffset % 16L))
        val size_0_offset = (this.memory_ID match {
            case SomeMut(v) =>
                32L
            case NoneMut() =>
                0L
        })
        val size_0_otherOffset = this.memory_ID match {
            case SomeMut(v) =>
                32L
            case NoneMut() =>
                0L
        }
        val size_1_offset = this.filePath.size(offset + size_0_offset)
        val size_1_otherOffset = this.filePath.size(otherOffset + size_0_otherOffset)
        this.filePath.sizeLemmaNextWord(offset + size_0_offset, otherOffset + size_0_otherOffset)
        val size_2_offset = 32L
        val size_2_otherOffset = 32L
        val size_3_offset = this.dataAreas.size(offset + size_0_offset + size_1_offset + size_2_offset)
        val size_3_otherOffset = this.dataAreas.size(otherOffset + size_0_otherOffset + size_1_otherOffset + size_2_otherOffset)
        this.dataAreas.sizeLemmaNextWord(offset + size_0_offset + size_1_offset + size_2_offset, otherOffset + size_0_otherOffset + size_1_otherOffset + size_2_otherOffset)
        ()
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextDWord(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854714357L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854714357L))
        require((offset % 32L) == (otherOffset % 32L))
        val size_0_offset = (this.memory_ID match {
            case SomeMut(v) =>
                32L
            case NoneMut() =>
                0L
        })
        val size_0_otherOffset = this.memory_ID match {
            case SomeMut(v) =>
                32L
            case NoneMut() =>
                0L
        }
        val size_1_offset = this.filePath.size(offset + size_0_offset)
        val size_1_otherOffset = this.filePath.size(otherOffset + size_0_otherOffset)
        this.filePath.sizeLemmaNextDWord(offset + size_0_offset, otherOffset + size_0_otherOffset)
        val size_2_offset = 32L
        val size_2_otherOffset = 32L
        val size_3_offset = this.dataAreas.size(offset + size_0_offset + size_1_offset + size_2_offset)
        val size_3_otherOffset = this.dataAreas.size(otherOffset + size_0_otherOffset + size_1_otherOffset + size_2_otherOffset)
        this.dataAreas.sizeLemmaNextDWord(offset + size_0_offset + size_1_offset + size_2_offset, otherOffset + size_0_otherOffset + size_1_otherOffset + size_2_otherOffset)
        ()
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }
}


@inline @cCode.inline val ERR_TC_6_20_DUMPRAWMEMORYDATAAREASTOFILE_DATAAREAS: Int = 813 /*(SIZE(1 .. maxTC-6-20-DataAreaCount)) */
@inline @cCode.inline val ERR_TC_6_20_DUMPRAWMEMORYDATAAREASTOFILE_DATAAREAS_ELM_2: Int = 808 /* */

@inline @cCode.inline val ERR_TC_6_20_DUMPRAWMEMORYDATAAREASTOFILE: Int = 818 /*(WITH COMPONENTS {memory-ID PRESENT}) */
@inline @cCode.inline val ERR_TC_6_20_DUMPRAWMEMORYDATAAREASTOFILE_MEMORY_ID_2: Int = 741 /* */
@inline @cCode.inline val ERR_TC_6_20_DUMPRAWMEMORYDATAAREASTOFILE_FILEPATH_2: Int = 771 /* */

 
 

@inline @cCode.inline val ERR_ACN_ENCODE_TC_6_20_DUMPRAWMEMORYDATAAREASTOFILE: Int = 821 /*  */
@inline @cCode.inline val ERR_ACN_ENCODE_TC_6_20_DUMPRAWMEMORYDATAAREASTOFILE_MEMORY_ID_2: Int = 744 /*  */
@inline @cCode.inline val ERR_ACN_ENCODE_TC_6_20_DUMPRAWMEMORYDATAAREASTOFILE_FILEPATH_2: Int = 774 /*  */
@inline @cCode.inline val ERR_ACN_ENCODE_TC_6_20_DUMPRAWMEMORYDATAAREASTOFILE_N_UNINITIALIZED: Int = 822 /*  */
@inline @cCode.inline val ERR_ACN_ENCODE_TC_6_20_DUMPRAWMEMORYDATAAREASTOFILE_N: Int = 776 /*  */
@inline @cCode.inline val ERR_ACN_ENCODE_TC_6_20_DUMPRAWMEMORYDATAAREASTOFILE_DATAAREAS: Int = 816 /*  */
@inline @cCode.inline val ERR_ACN_ENCODE_TC_6_20_DUMPRAWMEMORYDATAAREASTOFILE_DATAAREAS_ELM_2: Int = 811 /*  */
@inline @cCode.inline val TTC_6_20_DumpRawMemoryDataAreasToFile_REQUIRED_BYTES_FOR_ACN_ENCODING = 7682
@inline @cCode.inline val TTC_6_20_DumpRawMemoryDataAreasToFile_REQUIRED_BITS_FOR_ACN_ENCODING = 61450

@inline @cCode.inline val ERR_ACN_DECODE_TC_6_20_DUMPRAWMEMORYDATAAREASTOFILE: Int = 823 /*  */
@inline @cCode.inline val ERR_ACN_DECODE_TC_6_20_DUMPRAWMEMORYDATAAREASTOFILE_MEMORY_ID_2: Int = 745 /*  */
@inline @cCode.inline val ERR_ACN_DECODE_TC_6_20_DUMPRAWMEMORYDATAAREASTOFILE_FILEPATH_2: Int = 775 /*  */
@inline @cCode.inline val ERR_ACN_DECODE_TC_6_20_DUMPRAWMEMORYDATAAREASTOFILE_N: Int = 777 /*  */
@inline @cCode.inline val ERR_ACN_DECODE_TC_6_20_DUMPRAWMEMORYDATAAREASTOFILE_DATAAREAS: Int = 817 /*  */
@inline @cCode.inline val ERR_ACN_DECODE_TC_6_20_DUMPRAWMEMORYDATAAREASTOFILE_DATAAREAS_ELM_2: Int = 812 /*  */

// @inline @cCode.inline val maxTC_6_20_DataAreaCount: Int = 63 // defined in body

