/*
Code automatically generated by asn1scc tool
*/
package asn1src

import asn1scala._
import stainless.lang._
import stainless.annotation._
import stainless.collection._
import stainless.proof._
import StaticChecks._

/*-- TTM_2_6_RegisterDumpReport --------------------------------------------*/


case class TTM_2_6_RegisterDumpReport_registerDumps (nCount: Int, arr: Vector[TRegister])
{
    require(this.arr.size <= 63 && 1 <= this.nCount && this.nCount <= this.arr.size)

    def size(offset: Long): Long = {
        require((0L <= offset) && (offset <= 9223372036854773287L))
        TTM_2_6_RegisterDumpReport_registerDumps.sizeRange(this.arr, offset, 0, this.nCount)
    }.ensuring { (res: Long) => 
        (0L <= res) && (res <= 2520L)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaAnyOffset(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854773287L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854773287L))
        TTM_2_6_RegisterDumpReport_registerDumps.sizeLemmaAnyOffset(this.arr, offset, otherOffset, 0, this.nCount)
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextByte(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854773287L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854773287L))
        require((offset % 8L) == (otherOffset % 8L))
        TTM_2_6_RegisterDumpReport_registerDumps.sizeLemmaNextByte(this.arr, offset, otherOffset, 0, this.nCount)
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextWord(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854773287L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854773287L))
        require((offset % 16L) == (otherOffset % 16L))
        TTM_2_6_RegisterDumpReport_registerDumps.sizeLemmaNextWord(this.arr, offset, otherOffset, 0, this.nCount)
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextDWord(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854773287L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854773287L))
        require((offset % 32L) == (otherOffset % 32L))
        TTM_2_6_RegisterDumpReport_registerDumps.sizeLemmaNextDWord(this.arr, offset, otherOffset, 0, this.nCount)
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }
}
object TTM_2_6_RegisterDumpReport_registerDumps {
    def sizeRange(ls: Vector[TRegister], offset: Long, from: Int, to: Int): Long = {
        require((0 <= from) && (from <= to) && (to <= ls.size) && (ls.size <= 63))
        require((0L <= offset) && (offset <= 9223372036854775807L - 40L * (to - from)))
        decreases(to - from)
        if (from == to) {
            0L
        } else {
            val elemSize = ls.apply(from).size(offset)
            assert(0L <= elemSize && elemSize <= 40L)
            elemSize + TTM_2_6_RegisterDumpReport_registerDumps.sizeRange(ls, offset + elemSize, from + 1, to)
        }
    }.ensuring { (res: Long) => 
        val nbElems = (to - from)
        (0L <= res && res <= 40L * nbElems)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaAnyOffset(ls: Vector[TRegister], offset: Long, otherOffset: Long, from: Int, to: Int): Unit = {
        require((0 <= from) && (from <= to) && (to <= ls.size) && (ls.size <= 63))
        require((0L <= offset) && (offset <= 9223372036854775807L - 40L * (to - from)))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854775807L - 40L * (to - from)))
        decreases(to - from)
        if (from == to) {
            ()
        } else {
            val elemSizeOff = ls.apply(from).size(offset)
            val elemSizeOtherOff = ls.apply(from).size(otherOffset)
            assert(0L <= elemSizeOff && elemSizeOff <= 40L)
            assert(0L <= elemSizeOtherOff && elemSizeOtherOff <= 40L)
            ls.apply(from).sizeLemmaAnyOffset(offset, otherOffset)
            TTM_2_6_RegisterDumpReport_registerDumps.sizeLemmaAnyOffset(ls, offset + elemSizeOff, otherOffset + elemSizeOtherOff, from + 1, to)
        }
    }.ensuring { (_: Unit) => 
        TTM_2_6_RegisterDumpReport_registerDumps.sizeRange(ls, offset, from, to) == TTM_2_6_RegisterDumpReport_registerDumps.sizeRange(ls, otherOffset, from, to)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextByte(ls: Vector[TRegister], offset: Long, otherOffset: Long, from: Int, to: Int): Unit = {
        require((0 <= from) && (from <= to) && (to <= ls.size) && (ls.size <= 63))
        require((0L <= offset) && (offset <= 9223372036854775807L - 40L * (to - from)))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854775807L - 40L * (to - from)))
        require((offset % 8L) == (otherOffset % 8L))
        decreases(to - from)
        if (from == to) {
            ()
        } else {
            val elemSizeOff = ls.apply(from).size(offset)
            val elemSizeOtherOff = ls.apply(from).size(otherOffset)
            assert(0L <= elemSizeOff && elemSizeOff <= 40L)
            assert(0L <= elemSizeOtherOff && elemSizeOtherOff <= 40L)
            ls.apply(from).sizeLemmaNextByte(offset, otherOffset)
            TTM_2_6_RegisterDumpReport_registerDumps.sizeLemmaNextByte(ls, offset + elemSizeOff, otherOffset + elemSizeOtherOff, from + 1, to)
        }
    }.ensuring { (_: Unit) => 
        TTM_2_6_RegisterDumpReport_registerDumps.sizeRange(ls, offset, from, to) == TTM_2_6_RegisterDumpReport_registerDumps.sizeRange(ls, otherOffset, from, to)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextWord(ls: Vector[TRegister], offset: Long, otherOffset: Long, from: Int, to: Int): Unit = {
        require((0 <= from) && (from <= to) && (to <= ls.size) && (ls.size <= 63))
        require((0L <= offset) && (offset <= 9223372036854775807L - 40L * (to - from)))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854775807L - 40L * (to - from)))
        require((offset % 16L) == (otherOffset % 16L))
        decreases(to - from)
        if (from == to) {
            ()
        } else {
            val elemSizeOff = ls.apply(from).size(offset)
            val elemSizeOtherOff = ls.apply(from).size(otherOffset)
            assert(0L <= elemSizeOff && elemSizeOff <= 40L)
            assert(0L <= elemSizeOtherOff && elemSizeOtherOff <= 40L)
            ls.apply(from).sizeLemmaNextWord(offset, otherOffset)
            TTM_2_6_RegisterDumpReport_registerDumps.sizeLemmaNextWord(ls, offset + elemSizeOff, otherOffset + elemSizeOtherOff, from + 1, to)
        }
    }.ensuring { (_: Unit) => 
        TTM_2_6_RegisterDumpReport_registerDumps.sizeRange(ls, offset, from, to) == TTM_2_6_RegisterDumpReport_registerDumps.sizeRange(ls, otherOffset, from, to)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextDWord(ls: Vector[TRegister], offset: Long, otherOffset: Long, from: Int, to: Int): Unit = {
        require((0 <= from) && (from <= to) && (to <= ls.size) && (ls.size <= 63))
        require((0L <= offset) && (offset <= 9223372036854775807L - 40L * (to - from)))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854775807L - 40L * (to - from)))
        require((offset % 32L) == (otherOffset % 32L))
        decreases(to - from)
        if (from == to) {
            ()
        } else {
            val elemSizeOff = ls.apply(from).size(offset)
            val elemSizeOtherOff = ls.apply(from).size(otherOffset)
            assert(0L <= elemSizeOff && elemSizeOff <= 40L)
            assert(0L <= elemSizeOtherOff && elemSizeOtherOff <= 40L)
            ls.apply(from).sizeLemmaNextDWord(offset, otherOffset)
            TTM_2_6_RegisterDumpReport_registerDumps.sizeLemmaNextDWord(ls, offset + elemSizeOff, otherOffset + elemSizeOtherOff, from + 1, to)
        }
    }.ensuring { (_: Unit) => 
        TTM_2_6_RegisterDumpReport_registerDumps.sizeRange(ls, offset, from, to) == TTM_2_6_RegisterDumpReport_registerDumps.sizeRange(ls, otherOffset, from, to)
    }
}

case class TTM_2_6_RegisterDumpReport (
    registerDumps: TTM_2_6_RegisterDumpReport_registerDumps
) {

    def size(offset: Long): Long = {
        require((0L <= offset) && (offset <= 9223372036854773255L))
        val size_0 = 32L
        val size_1 = this.registerDumps.size(offset + size_0)
        size_0 + size_1
    }.ensuring { (res: Long) => 
        (0L <= res) && (res <= 2552L)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaAnyOffset(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854773255L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854773255L))
        val size_0_offset = 32L
        val size_0_otherOffset = 32L
        val size_1_offset = this.registerDumps.size(offset + size_0_offset)
        val size_1_otherOffset = this.registerDumps.size(otherOffset + size_0_otherOffset)
        this.registerDumps.sizeLemmaAnyOffset(offset + size_0_offset, otherOffset + size_0_otherOffset)
        ()
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextByte(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854773255L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854773255L))
        require((offset % 8L) == (otherOffset % 8L))
        val size_0_offset = 32L
        val size_0_otherOffset = 32L
        val size_1_offset = this.registerDumps.size(offset + size_0_offset)
        val size_1_otherOffset = this.registerDumps.size(otherOffset + size_0_otherOffset)
        this.registerDumps.sizeLemmaNextByte(offset + size_0_offset, otherOffset + size_0_otherOffset)
        ()
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextWord(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854773255L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854773255L))
        require((offset % 16L) == (otherOffset % 16L))
        val size_0_offset = 32L
        val size_0_otherOffset = 32L
        val size_1_offset = this.registerDumps.size(offset + size_0_offset)
        val size_1_otherOffset = this.registerDumps.size(otherOffset + size_0_otherOffset)
        this.registerDumps.sizeLemmaNextWord(offset + size_0_offset, otherOffset + size_0_otherOffset)
        ()
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextDWord(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854773255L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854773255L))
        require((offset % 32L) == (otherOffset % 32L))
        val size_0_offset = 32L
        val size_0_otherOffset = 32L
        val size_1_offset = this.registerDumps.size(offset + size_0_offset)
        val size_1_otherOffset = this.registerDumps.size(otherOffset + size_0_otherOffset)
        this.registerDumps.sizeLemmaNextDWord(offset + size_0_offset, otherOffset + size_0_otherOffset)
        ()
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }
}


@inline @cCode.inline val ERR_TM_2_6_REGISTERDUMPREPORT_REGISTERDUMPS: Int = 665 /*(SIZE(1 .. maxTC-2-6-RegisterDumpCount)) */
@inline @cCode.inline val ERR_TM_2_6_REGISTERDUMPREPORT_REGISTERDUMPS_ELM_2: Int = 660 /* */

@inline @cCode.inline val ERR_TM_2_6_REGISTERDUMPREPORT: Int = 670 /* */

 
 

@inline @cCode.inline val ERR_ACN_ENCODE_TM_2_6_REGISTERDUMPREPORT: Int = 673 /*  */
@inline @cCode.inline val ERR_ACN_ENCODE_TM_2_6_REGISTERDUMPREPORT_N_UNINITIALIZED: Int = 674 /*  */
@inline @cCode.inline val ERR_ACN_ENCODE_TM_2_6_REGISTERDUMPREPORT_N: Int = 633 /*  */
@inline @cCode.inline val ERR_ACN_ENCODE_TM_2_6_REGISTERDUMPREPORT_REGISTERDUMPS: Int = 668 /*  */
@inline @cCode.inline val ERR_ACN_ENCODE_TM_2_6_REGISTERDUMPREPORT_REGISTERDUMPS_ELM_2: Int = 663 /*  */
@inline @cCode.inline val TTM_2_6_RegisterDumpReport_REQUIRED_BYTES_FOR_ACN_ENCODING = 319
@inline @cCode.inline val TTM_2_6_RegisterDumpReport_REQUIRED_BITS_FOR_ACN_ENCODING = 2552

@inline @cCode.inline val ERR_ACN_DECODE_TM_2_6_REGISTERDUMPREPORT: Int = 675 /*  */
@inline @cCode.inline val ERR_ACN_DECODE_TM_2_6_REGISTERDUMPREPORT_N: Int = 634 /*  */
@inline @cCode.inline val ERR_ACN_DECODE_TM_2_6_REGISTERDUMPREPORT_REGISTERDUMPS: Int = 669 /*  */
@inline @cCode.inline val ERR_ACN_DECODE_TM_2_6_REGISTERDUMPREPORT_REGISTERDUMPS_ELM_2: Int = 664 /*  */

// @inline @cCode.inline val maxTC_2_6_RegisterDumpCount: Int = 63 // defined in body

