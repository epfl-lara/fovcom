/*
Code automatically generated by asn1scc tool
*/
package asn1src

import asn1scala._
import stainless.lang._
import stainless.annotation._
import stainless.collection._
import stainless.proof._
import StaticChecks._

/*-- TTC_2_5_DistributeRegisterDumpCommands --------------------------------------------*/


case class TTC_2_5_DistributeRegisterDumpCommands_registerAddresses (nCount: Int, arr: Vector[TRegisterAddress])
{
    require(this.arr.size <= 63 && 1 <= this.nCount && this.nCount <= this.arr.size)

    def size(offset: Long): Long = {
        require((0L <= offset) && (offset <= 9223372036854773791L))
        TTC_2_5_DistributeRegisterDumpCommands_registerAddresses.sizeRange(this.arr, offset, 0, this.nCount)
    }.ensuring { (res: Long) => 
        (0L <= res) && (res <= 2016L)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaAnyOffset(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854773791L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854773791L))
        TTC_2_5_DistributeRegisterDumpCommands_registerAddresses.sizeLemmaAnyOffset(this.arr, offset, otherOffset, 0, this.nCount)
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextByte(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854773791L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854773791L))
        require((offset % 8L) == (otherOffset % 8L))
        TTC_2_5_DistributeRegisterDumpCommands_registerAddresses.sizeLemmaNextByte(this.arr, offset, otherOffset, 0, this.nCount)
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextWord(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854773791L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854773791L))
        require((offset % 16L) == (otherOffset % 16L))
        TTC_2_5_DistributeRegisterDumpCommands_registerAddresses.sizeLemmaNextWord(this.arr, offset, otherOffset, 0, this.nCount)
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextDWord(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854773791L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854773791L))
        require((offset % 32L) == (otherOffset % 32L))
        TTC_2_5_DistributeRegisterDumpCommands_registerAddresses.sizeLemmaNextDWord(this.arr, offset, otherOffset, 0, this.nCount)
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }
}
object TTC_2_5_DistributeRegisterDumpCommands_registerAddresses {
    def sizeRange(ls: Vector[TRegisterAddress], offset: Long, from: Int, to: Int): Long = {
        require((0 <= from) && (from <= to) && (to <= ls.size) && (ls.size <= 63))
        require((0L <= offset) && (offset <= 9223372036854775807L - 32L * (to - from)))
        decreases(to - from)
        if (from == to) {
            0L
        } else {
            val elemSize = 32L
            assert(elemSize == 32L)
            elemSize + TTC_2_5_DistributeRegisterDumpCommands_registerAddresses.sizeRange(ls, offset + elemSize, from + 1, to)
        }
    }.ensuring { (res: Long) => 
        val nbElems = (to - from)
        (0L <= res && res <= 32L * nbElems)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaAnyOffset(ls: Vector[TRegisterAddress], offset: Long, otherOffset: Long, from: Int, to: Int): Unit = {
        require((0 <= from) && (from <= to) && (to <= ls.size) && (ls.size <= 63))
        require((0L <= offset) && (offset <= 9223372036854775807L - 32L * (to - from)))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854775807L - 32L * (to - from)))
        decreases(to - from)
        if (from == to) {
            ()
        } else {
            val elemSizeOff = 32L
            val elemSizeOtherOff = 32L
            assert(elemSizeOff == 32L)
            assert(elemSizeOtherOff == 32L)
            TTC_2_5_DistributeRegisterDumpCommands_registerAddresses.sizeLemmaAnyOffset(ls, offset + elemSizeOff, otherOffset + elemSizeOtherOff, from + 1, to)
        }
    }.ensuring { (_: Unit) => 
        TTC_2_5_DistributeRegisterDumpCommands_registerAddresses.sizeRange(ls, offset, from, to) == TTC_2_5_DistributeRegisterDumpCommands_registerAddresses.sizeRange(ls, otherOffset, from, to)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextByte(ls: Vector[TRegisterAddress], offset: Long, otherOffset: Long, from: Int, to: Int): Unit = {
        require((0 <= from) && (from <= to) && (to <= ls.size) && (ls.size <= 63))
        require((0L <= offset) && (offset <= 9223372036854775807L - 32L * (to - from)))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854775807L - 32L * (to - from)))
        require((offset % 8L) == (otherOffset % 8L))
        decreases(to - from)
        if (from == to) {
            ()
        } else {
            val elemSizeOff = 32L
            val elemSizeOtherOff = 32L
            assert(elemSizeOff == 32L)
            assert(elemSizeOtherOff == 32L)
            TTC_2_5_DistributeRegisterDumpCommands_registerAddresses.sizeLemmaNextByte(ls, offset + elemSizeOff, otherOffset + elemSizeOtherOff, from + 1, to)
        }
    }.ensuring { (_: Unit) => 
        TTC_2_5_DistributeRegisterDumpCommands_registerAddresses.sizeRange(ls, offset, from, to) == TTC_2_5_DistributeRegisterDumpCommands_registerAddresses.sizeRange(ls, otherOffset, from, to)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextWord(ls: Vector[TRegisterAddress], offset: Long, otherOffset: Long, from: Int, to: Int): Unit = {
        require((0 <= from) && (from <= to) && (to <= ls.size) && (ls.size <= 63))
        require((0L <= offset) && (offset <= 9223372036854775807L - 32L * (to - from)))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854775807L - 32L * (to - from)))
        require((offset % 16L) == (otherOffset % 16L))
        decreases(to - from)
        if (from == to) {
            ()
        } else {
            val elemSizeOff = 32L
            val elemSizeOtherOff = 32L
            assert(elemSizeOff == 32L)
            assert(elemSizeOtherOff == 32L)
            TTC_2_5_DistributeRegisterDumpCommands_registerAddresses.sizeLemmaNextWord(ls, offset + elemSizeOff, otherOffset + elemSizeOtherOff, from + 1, to)
        }
    }.ensuring { (_: Unit) => 
        TTC_2_5_DistributeRegisterDumpCommands_registerAddresses.sizeRange(ls, offset, from, to) == TTC_2_5_DistributeRegisterDumpCommands_registerAddresses.sizeRange(ls, otherOffset, from, to)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextDWord(ls: Vector[TRegisterAddress], offset: Long, otherOffset: Long, from: Int, to: Int): Unit = {
        require((0 <= from) && (from <= to) && (to <= ls.size) && (ls.size <= 63))
        require((0L <= offset) && (offset <= 9223372036854775807L - 32L * (to - from)))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854775807L - 32L * (to - from)))
        require((offset % 32L) == (otherOffset % 32L))
        decreases(to - from)
        if (from == to) {
            ()
        } else {
            val elemSizeOff = 32L
            val elemSizeOtherOff = 32L
            assert(elemSizeOff == 32L)
            assert(elemSizeOtherOff == 32L)
            TTC_2_5_DistributeRegisterDumpCommands_registerAddresses.sizeLemmaNextDWord(ls, offset + elemSizeOff, otherOffset + elemSizeOtherOff, from + 1, to)
        }
    }.ensuring { (_: Unit) => 
        TTC_2_5_DistributeRegisterDumpCommands_registerAddresses.sizeRange(ls, offset, from, to) == TTC_2_5_DistributeRegisterDumpCommands_registerAddresses.sizeRange(ls, otherOffset, from, to)
    }
}

case class TTC_2_5_DistributeRegisterDumpCommands (
    registerAddresses: TTC_2_5_DistributeRegisterDumpCommands_registerAddresses
) {

    def size(offset: Long): Long = {
        require((0L <= offset) && (offset <= 9223372036854773759L))
        val size_0 = 32L
        val size_1 = this.registerAddresses.size(offset + size_0)
        size_0 + size_1
    }.ensuring { (res: Long) => 
        (0L <= res) && (res <= 2048L)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaAnyOffset(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854773759L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854773759L))
        val size_0_offset = 32L
        val size_0_otherOffset = 32L
        val size_1_offset = this.registerAddresses.size(offset + size_0_offset)
        val size_1_otherOffset = this.registerAddresses.size(otherOffset + size_0_otherOffset)
        this.registerAddresses.sizeLemmaAnyOffset(offset + size_0_offset, otherOffset + size_0_otherOffset)
        ()
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextByte(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854773759L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854773759L))
        require((offset % 8L) == (otherOffset % 8L))
        val size_0_offset = 32L
        val size_0_otherOffset = 32L
        val size_1_offset = this.registerAddresses.size(offset + size_0_offset)
        val size_1_otherOffset = this.registerAddresses.size(otherOffset + size_0_otherOffset)
        this.registerAddresses.sizeLemmaNextByte(offset + size_0_offset, otherOffset + size_0_otherOffset)
        ()
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextWord(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854773759L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854773759L))
        require((offset % 16L) == (otherOffset % 16L))
        val size_0_offset = 32L
        val size_0_otherOffset = 32L
        val size_1_offset = this.registerAddresses.size(offset + size_0_offset)
        val size_1_otherOffset = this.registerAddresses.size(otherOffset + size_0_otherOffset)
        this.registerAddresses.sizeLemmaNextWord(offset + size_0_offset, otherOffset + size_0_otherOffset)
        ()
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextDWord(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854773759L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854773759L))
        require((offset % 32L) == (otherOffset % 32L))
        val size_0_offset = 32L
        val size_0_otherOffset = 32L
        val size_1_offset = this.registerAddresses.size(offset + size_0_offset)
        val size_1_otherOffset = this.registerAddresses.size(otherOffset + size_0_otherOffset)
        this.registerAddresses.sizeLemmaNextDWord(offset + size_0_offset, otherOffset + size_0_otherOffset)
        ()
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }
}


@inline @cCode.inline val ERR_TC_2_5_DISTRIBUTEREGISTERDUMPCOMMANDS_REGISTERADDRESSES: Int = 622 /*(SIZE(1 .. maxTC-2-5-RegisterAddressCount)) */
@inline @cCode.inline val ERR_TC_2_5_DISTRIBUTEREGISTERDUMPCOMMANDS_REGISTERADDRESSES_ELM_2: Int = 617 /* */

@inline @cCode.inline val ERR_TC_2_5_DISTRIBUTEREGISTERDUMPCOMMANDS: Int = 627 /* */

 
 

@inline @cCode.inline val ERR_ACN_ENCODE_TC_2_5_DISTRIBUTEREGISTERDUMPCOMMANDS: Int = 630 /*  */
@inline @cCode.inline val ERR_ACN_ENCODE_TC_2_5_DISTRIBUTEREGISTERDUMPCOMMANDS_N_UNINITIALIZED: Int = 631 /*  */
@inline @cCode.inline val ERR_ACN_ENCODE_TC_2_5_DISTRIBUTEREGISTERDUMPCOMMANDS_N: Int = 610 /*  */
@inline @cCode.inline val ERR_ACN_ENCODE_TC_2_5_DISTRIBUTEREGISTERDUMPCOMMANDS_REGISTERADDRESSES: Int = 625 /*  */
@inline @cCode.inline val ERR_ACN_ENCODE_TC_2_5_DISTRIBUTEREGISTERDUMPCOMMANDS_REGISTERADDRESSES_ELM_2: Int = 620 /*  */
@inline @cCode.inline val TTC_2_5_DistributeRegisterDumpCommands_REQUIRED_BYTES_FOR_ACN_ENCODING = 256
@inline @cCode.inline val TTC_2_5_DistributeRegisterDumpCommands_REQUIRED_BITS_FOR_ACN_ENCODING = 2048

@inline @cCode.inline val ERR_ACN_DECODE_TC_2_5_DISTRIBUTEREGISTERDUMPCOMMANDS: Int = 632 /*  */
@inline @cCode.inline val ERR_ACN_DECODE_TC_2_5_DISTRIBUTEREGISTERDUMPCOMMANDS_N: Int = 611 /*  */
@inline @cCode.inline val ERR_ACN_DECODE_TC_2_5_DISTRIBUTEREGISTERDUMPCOMMANDS_REGISTERADDRESSES: Int = 626 /*  */
@inline @cCode.inline val ERR_ACN_DECODE_TC_2_5_DISTRIBUTEREGISTERDUMPCOMMANDS_REGISTERADDRESSES_ELM_2: Int = 621 /*  */

// @inline @cCode.inline val maxTC_2_5_RegisterAddressCount: Int = 63 // defined in body

