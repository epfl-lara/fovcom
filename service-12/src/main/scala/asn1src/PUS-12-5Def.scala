/*
Code automatically generated by asn1scc tool
*/
package asn1src

import asn1scala._
import stainless.lang._
import stainless.annotation._
import stainless.collection._
import stainless.proof._
import StaticChecks._

/*-- TTC_12_5_AddParameterMonitoringDefinitions --------------------------------------------*/


case class TTC_12_5_AddParameterMonitoringDefinitions_definitions (nCount: Int, arr: Vector[TParameterMonitoringDefinition])
{
    require(this.arr.size <= 4095 && 1 <= this.nCount && this.nCount <= this.arr.size)

    def size(offset: Long): Long = {
        require((0L <= offset) && (offset <= 9223372036853588257L))
        TTC_12_5_AddParameterMonitoringDefinitions_definitions.sizeRange(this.arr, offset, 0, this.nCount)
    }.ensuring { (res: Long) => 
        (0L <= res) && (res <= 1187550L)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaAnyOffset(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036853588257L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036853588257L))
        TTC_12_5_AddParameterMonitoringDefinitions_definitions.sizeLemmaAnyOffset(this.arr, offset, otherOffset, 0, this.nCount)
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextByte(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036853588257L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036853588257L))
        require((offset % 8L) == (otherOffset % 8L))
        TTC_12_5_AddParameterMonitoringDefinitions_definitions.sizeLemmaNextByte(this.arr, offset, otherOffset, 0, this.nCount)
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextWord(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036853588257L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036853588257L))
        require((offset % 16L) == (otherOffset % 16L))
        TTC_12_5_AddParameterMonitoringDefinitions_definitions.sizeLemmaNextWord(this.arr, offset, otherOffset, 0, this.nCount)
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextDWord(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036853588257L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036853588257L))
        require((offset % 32L) == (otherOffset % 32L))
        TTC_12_5_AddParameterMonitoringDefinitions_definitions.sizeLemmaNextDWord(this.arr, offset, otherOffset, 0, this.nCount)
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }
}
object TTC_12_5_AddParameterMonitoringDefinitions_definitions {
    def sizeRange(ls: Vector[TParameterMonitoringDefinition], offset: Long, from: Int, to: Int): Long = {
        require((0 <= from) && (from <= to) && (to <= ls.size) && (ls.size <= 4095))
        require((0L <= offset) && (offset <= 9223372036854775807L - 290L * (to - from)))
        decreases(to - from)
        if (from == to) {
            0L
        } else {
            val elemSize = ls.apply(from).size(offset)
            assert(0L <= elemSize && elemSize <= 290L)
            elemSize + TTC_12_5_AddParameterMonitoringDefinitions_definitions.sizeRange(ls, offset + elemSize, from + 1, to)
        }
    }.ensuring { (res: Long) => 
        val nbElems = (to - from)
        (0L <= res && res <= 290L * nbElems)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaAnyOffset(ls: Vector[TParameterMonitoringDefinition], offset: Long, otherOffset: Long, from: Int, to: Int): Unit = {
        require((0 <= from) && (from <= to) && (to <= ls.size) && (ls.size <= 4095))
        require((0L <= offset) && (offset <= 9223372036854775807L - 290L * (to - from)))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854775807L - 290L * (to - from)))
        decreases(to - from)
        if (from == to) {
            ()
        } else {
            val elemSizeOff = ls.apply(from).size(offset)
            val elemSizeOtherOff = ls.apply(from).size(otherOffset)
            assert(0L <= elemSizeOff && elemSizeOff <= 290L)
            assert(0L <= elemSizeOtherOff && elemSizeOtherOff <= 290L)
            ls.apply(from).sizeLemmaAnyOffset(offset, otherOffset)
            TTC_12_5_AddParameterMonitoringDefinitions_definitions.sizeLemmaAnyOffset(ls, offset + elemSizeOff, otherOffset + elemSizeOtherOff, from + 1, to)
        }
    }.ensuring { (_: Unit) => 
        TTC_12_5_AddParameterMonitoringDefinitions_definitions.sizeRange(ls, offset, from, to) == TTC_12_5_AddParameterMonitoringDefinitions_definitions.sizeRange(ls, otherOffset, from, to)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextByte(ls: Vector[TParameterMonitoringDefinition], offset: Long, otherOffset: Long, from: Int, to: Int): Unit = {
        require((0 <= from) && (from <= to) && (to <= ls.size) && (ls.size <= 4095))
        require((0L <= offset) && (offset <= 9223372036854775807L - 290L * (to - from)))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854775807L - 290L * (to - from)))
        require((offset % 8L) == (otherOffset % 8L))
        decreases(to - from)
        if (from == to) {
            ()
        } else {
            val elemSizeOff = ls.apply(from).size(offset)
            val elemSizeOtherOff = ls.apply(from).size(otherOffset)
            assert(0L <= elemSizeOff && elemSizeOff <= 290L)
            assert(0L <= elemSizeOtherOff && elemSizeOtherOff <= 290L)
            ls.apply(from).sizeLemmaNextByte(offset, otherOffset)
            TTC_12_5_AddParameterMonitoringDefinitions_definitions.sizeLemmaNextByte(ls, offset + elemSizeOff, otherOffset + elemSizeOtherOff, from + 1, to)
        }
    }.ensuring { (_: Unit) => 
        TTC_12_5_AddParameterMonitoringDefinitions_definitions.sizeRange(ls, offset, from, to) == TTC_12_5_AddParameterMonitoringDefinitions_definitions.sizeRange(ls, otherOffset, from, to)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextWord(ls: Vector[TParameterMonitoringDefinition], offset: Long, otherOffset: Long, from: Int, to: Int): Unit = {
        require((0 <= from) && (from <= to) && (to <= ls.size) && (ls.size <= 4095))
        require((0L <= offset) && (offset <= 9223372036854775807L - 290L * (to - from)))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854775807L - 290L * (to - from)))
        require((offset % 16L) == (otherOffset % 16L))
        decreases(to - from)
        if (from == to) {
            ()
        } else {
            val elemSizeOff = ls.apply(from).size(offset)
            val elemSizeOtherOff = ls.apply(from).size(otherOffset)
            assert(0L <= elemSizeOff && elemSizeOff <= 290L)
            assert(0L <= elemSizeOtherOff && elemSizeOtherOff <= 290L)
            ls.apply(from).sizeLemmaNextWord(offset, otherOffset)
            TTC_12_5_AddParameterMonitoringDefinitions_definitions.sizeLemmaNextWord(ls, offset + elemSizeOff, otherOffset + elemSizeOtherOff, from + 1, to)
        }
    }.ensuring { (_: Unit) => 
        TTC_12_5_AddParameterMonitoringDefinitions_definitions.sizeRange(ls, offset, from, to) == TTC_12_5_AddParameterMonitoringDefinitions_definitions.sizeRange(ls, otherOffset, from, to)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextDWord(ls: Vector[TParameterMonitoringDefinition], offset: Long, otherOffset: Long, from: Int, to: Int): Unit = {
        require((0 <= from) && (from <= to) && (to <= ls.size) && (ls.size <= 4095))
        require((0L <= offset) && (offset <= 9223372036854775807L - 290L * (to - from)))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854775807L - 290L * (to - from)))
        require((offset % 32L) == (otherOffset % 32L))
        decreases(to - from)
        if (from == to) {
            ()
        } else {
            val elemSizeOff = ls.apply(from).size(offset)
            val elemSizeOtherOff = ls.apply(from).size(otherOffset)
            assert(0L <= elemSizeOff && elemSizeOff <= 290L)
            assert(0L <= elemSizeOtherOff && elemSizeOtherOff <= 290L)
            ls.apply(from).sizeLemmaNextDWord(offset, otherOffset)
            TTC_12_5_AddParameterMonitoringDefinitions_definitions.sizeLemmaNextDWord(ls, offset + elemSizeOff, otherOffset + elemSizeOtherOff, from + 1, to)
        }
    }.ensuring { (_: Unit) => 
        TTC_12_5_AddParameterMonitoringDefinitions_definitions.sizeRange(ls, offset, from, to) == TTC_12_5_AddParameterMonitoringDefinitions_definitions.sizeRange(ls, otherOffset, from, to)
    }
}

case class TTC_12_5_AddParameterMonitoringDefinitions (
    definitions: TTC_12_5_AddParameterMonitoringDefinitions_definitions
) {

    def size(offset: Long): Long = {
        require((0L <= offset) && (offset <= 9223372036853588225L))
        val size_0 = 32L
        val size_1 = this.definitions.size(offset + size_0)
        size_0 + size_1
    }.ensuring { (res: Long) => 
        (0L <= res) && (res <= 1187582L)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaAnyOffset(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036853588225L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036853588225L))
        val size_0_offset = 32L
        val size_0_otherOffset = 32L
        val size_1_offset = this.definitions.size(offset + size_0_offset)
        val size_1_otherOffset = this.definitions.size(otherOffset + size_0_otherOffset)
        this.definitions.sizeLemmaAnyOffset(offset + size_0_offset, otherOffset + size_0_otherOffset)
        ()
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextByte(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036853588225L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036853588225L))
        require((offset % 8L) == (otherOffset % 8L))
        val size_0_offset = 32L
        val size_0_otherOffset = 32L
        val size_1_offset = this.definitions.size(offset + size_0_offset)
        val size_1_otherOffset = this.definitions.size(otherOffset + size_0_otherOffset)
        this.definitions.sizeLemmaNextByte(offset + size_0_offset, otherOffset + size_0_otherOffset)
        ()
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextWord(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036853588225L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036853588225L))
        require((offset % 16L) == (otherOffset % 16L))
        val size_0_offset = 32L
        val size_0_otherOffset = 32L
        val size_1_offset = this.definitions.size(offset + size_0_offset)
        val size_1_otherOffset = this.definitions.size(otherOffset + size_0_otherOffset)
        this.definitions.sizeLemmaNextWord(offset + size_0_offset, otherOffset + size_0_otherOffset)
        ()
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextDWord(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036853588225L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036853588225L))
        require((offset % 32L) == (otherOffset % 32L))
        val size_0_offset = 32L
        val size_0_otherOffset = 32L
        val size_1_offset = this.definitions.size(offset + size_0_offset)
        val size_1_otherOffset = this.definitions.size(otherOffset + size_0_otherOffset)
        this.definitions.sizeLemmaNextDWord(offset + size_0_offset, otherOffset + size_0_otherOffset)
        ()
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }
}


@inline @cCode.inline val ERR_TC_12_5_ADDPARAMETERMONITORINGDEFINITIONS_DEFINITIONS: Int = 2496 /*(SIZE(1 .. maxTC-12-5-ParameterMonitoringDefinitionCount)) */
@inline @cCode.inline val ERR_TC_12_5_ADDPARAMETERMONITORINGDEFINITIONS_DEFINITIONS_ELM_2: Int = 2491 /* */

@inline @cCode.inline val ERR_TC_12_5_ADDPARAMETERMONITORINGDEFINITIONS: Int = 2501 /* */

 
 

@inline @cCode.inline val ERR_ACN_ENCODE_TC_12_5_ADDPARAMETERMONITORINGDEFINITIONS: Int = 2504 /*  */
@inline @cCode.inline val ERR_ACN_ENCODE_TC_12_5_ADDPARAMETERMONITORINGDEFINITIONS_N_UNINITIALIZED: Int = 2505 /*  */
@inline @cCode.inline val ERR_ACN_ENCODE_TC_12_5_ADDPARAMETERMONITORINGDEFINITIONS_N: Int = 2235 /*  */
@inline @cCode.inline val ERR_ACN_ENCODE_TC_12_5_ADDPARAMETERMONITORINGDEFINITIONS_DEFINITIONS: Int = 2499 /*  */
@inline @cCode.inline val ERR_ACN_ENCODE_TC_12_5_ADDPARAMETERMONITORINGDEFINITIONS_DEFINITIONS_ELM_2: Int = 2494 /*  */
@inline @cCode.inline val TTC_12_5_AddParameterMonitoringDefinitions_REQUIRED_BYTES_FOR_ACN_ENCODING = 148448
@inline @cCode.inline val TTC_12_5_AddParameterMonitoringDefinitions_REQUIRED_BITS_FOR_ACN_ENCODING = 1187582

@inline @cCode.inline val ERR_ACN_DECODE_TC_12_5_ADDPARAMETERMONITORINGDEFINITIONS: Int = 2506 /*  */
@inline @cCode.inline val ERR_ACN_DECODE_TC_12_5_ADDPARAMETERMONITORINGDEFINITIONS_N: Int = 2236 /*  */
@inline @cCode.inline val ERR_ACN_DECODE_TC_12_5_ADDPARAMETERMONITORINGDEFINITIONS_DEFINITIONS: Int = 2500 /*  */
@inline @cCode.inline val ERR_ACN_DECODE_TC_12_5_ADDPARAMETERMONITORINGDEFINITIONS_DEFINITIONS_ELM_2: Int = 2495 /*  */

// @inline @cCode.inline val maxTC_12_5_ParameterMonitoringDefinitionCount: Int = 4095 // defined in body

