/*
Code automatically generated by asn1scc tool
*/
package asn1src

import asn1scala._
import stainless.lang._
import stainless.annotation._
import stainless.collection._
import stainless.proof._
import StaticChecks._

/*-- TTC_11_24_EnableTimeBasedSchedulingGroups --------------------------------------------*/


case class TTC_11_24_EnableTimeBasedSchedulingGroups_groups (nCount: Int, arr: Vector[TGroup_ID])
{
    require(this.arr.size <= 4095 && 0 <= this.nCount && this.nCount <= this.arr.size)

    def size(offset: Long): Long = {
        require((0L <= offset) && (offset <= 9223372036854644767L))
        TTC_11_24_EnableTimeBasedSchedulingGroups_groups.sizeRange(this.arr, offset, 0, this.nCount)
    }.ensuring { (res: Long) => 
        (0L <= res) && (res <= 131040L)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaAnyOffset(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854644767L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854644767L))
        TTC_11_24_EnableTimeBasedSchedulingGroups_groups.sizeLemmaAnyOffset(this.arr, offset, otherOffset, 0, this.nCount)
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextByte(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854644767L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854644767L))
        require((offset % 8L) == (otherOffset % 8L))
        TTC_11_24_EnableTimeBasedSchedulingGroups_groups.sizeLemmaNextByte(this.arr, offset, otherOffset, 0, this.nCount)
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextWord(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854644767L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854644767L))
        require((offset % 16L) == (otherOffset % 16L))
        TTC_11_24_EnableTimeBasedSchedulingGroups_groups.sizeLemmaNextWord(this.arr, offset, otherOffset, 0, this.nCount)
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextDWord(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854644767L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854644767L))
        require((offset % 32L) == (otherOffset % 32L))
        TTC_11_24_EnableTimeBasedSchedulingGroups_groups.sizeLemmaNextDWord(this.arr, offset, otherOffset, 0, this.nCount)
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }
}
object TTC_11_24_EnableTimeBasedSchedulingGroups_groups {
    def sizeRange(ls: Vector[ULong], offset: Long, from: Int, to: Int): Long = {
        require((0 <= from) && (from <= to) && (to <= ls.size) && (ls.size <= 4095))
        require((0L <= offset) && (offset <= 9223372036854775807L - 32L * (to - from)))
        decreases(to - from)
        if (from == to) {
            0L
        } else {
            val elemSize = 32L
            assert(elemSize == 32L)
            elemSize + TTC_11_24_EnableTimeBasedSchedulingGroups_groups.sizeRange(ls, offset + elemSize, from + 1, to)
        }
    }.ensuring { (res: Long) => 
        val nbElems = (to - from)
        (0L <= res && res <= 32L * nbElems)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaAnyOffset(ls: Vector[ULong], offset: Long, otherOffset: Long, from: Int, to: Int): Unit = {
        require((0 <= from) && (from <= to) && (to <= ls.size) && (ls.size <= 4095))
        require((0L <= offset) && (offset <= 9223372036854775807L - 32L * (to - from)))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854775807L - 32L * (to - from)))
        decreases(to - from)
        if (from == to) {
            ()
        } else {
            val elemSizeOff = 32L
            val elemSizeOtherOff = 32L
            assert(elemSizeOff == 32L)
            assert(elemSizeOtherOff == 32L)
            TTC_11_24_EnableTimeBasedSchedulingGroups_groups.sizeLemmaAnyOffset(ls, offset + elemSizeOff, otherOffset + elemSizeOtherOff, from + 1, to)
        }
    }.ensuring { (_: Unit) => 
        TTC_11_24_EnableTimeBasedSchedulingGroups_groups.sizeRange(ls, offset, from, to) == TTC_11_24_EnableTimeBasedSchedulingGroups_groups.sizeRange(ls, otherOffset, from, to)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextByte(ls: Vector[ULong], offset: Long, otherOffset: Long, from: Int, to: Int): Unit = {
        require((0 <= from) && (from <= to) && (to <= ls.size) && (ls.size <= 4095))
        require((0L <= offset) && (offset <= 9223372036854775807L - 32L * (to - from)))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854775807L - 32L * (to - from)))
        require((offset % 8L) == (otherOffset % 8L))
        decreases(to - from)
        if (from == to) {
            ()
        } else {
            val elemSizeOff = 32L
            val elemSizeOtherOff = 32L
            assert(elemSizeOff == 32L)
            assert(elemSizeOtherOff == 32L)
            TTC_11_24_EnableTimeBasedSchedulingGroups_groups.sizeLemmaNextByte(ls, offset + elemSizeOff, otherOffset + elemSizeOtherOff, from + 1, to)
        }
    }.ensuring { (_: Unit) => 
        TTC_11_24_EnableTimeBasedSchedulingGroups_groups.sizeRange(ls, offset, from, to) == TTC_11_24_EnableTimeBasedSchedulingGroups_groups.sizeRange(ls, otherOffset, from, to)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextWord(ls: Vector[ULong], offset: Long, otherOffset: Long, from: Int, to: Int): Unit = {
        require((0 <= from) && (from <= to) && (to <= ls.size) && (ls.size <= 4095))
        require((0L <= offset) && (offset <= 9223372036854775807L - 32L * (to - from)))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854775807L - 32L * (to - from)))
        require((offset % 16L) == (otherOffset % 16L))
        decreases(to - from)
        if (from == to) {
            ()
        } else {
            val elemSizeOff = 32L
            val elemSizeOtherOff = 32L
            assert(elemSizeOff == 32L)
            assert(elemSizeOtherOff == 32L)
            TTC_11_24_EnableTimeBasedSchedulingGroups_groups.sizeLemmaNextWord(ls, offset + elemSizeOff, otherOffset + elemSizeOtherOff, from + 1, to)
        }
    }.ensuring { (_: Unit) => 
        TTC_11_24_EnableTimeBasedSchedulingGroups_groups.sizeRange(ls, offset, from, to) == TTC_11_24_EnableTimeBasedSchedulingGroups_groups.sizeRange(ls, otherOffset, from, to)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextDWord(ls: Vector[ULong], offset: Long, otherOffset: Long, from: Int, to: Int): Unit = {
        require((0 <= from) && (from <= to) && (to <= ls.size) && (ls.size <= 4095))
        require((0L <= offset) && (offset <= 9223372036854775807L - 32L * (to - from)))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854775807L - 32L * (to - from)))
        require((offset % 32L) == (otherOffset % 32L))
        decreases(to - from)
        if (from == to) {
            ()
        } else {
            val elemSizeOff = 32L
            val elemSizeOtherOff = 32L
            assert(elemSizeOff == 32L)
            assert(elemSizeOtherOff == 32L)
            TTC_11_24_EnableTimeBasedSchedulingGroups_groups.sizeLemmaNextDWord(ls, offset + elemSizeOff, otherOffset + elemSizeOtherOff, from + 1, to)
        }
    }.ensuring { (_: Unit) => 
        TTC_11_24_EnableTimeBasedSchedulingGroups_groups.sizeRange(ls, offset, from, to) == TTC_11_24_EnableTimeBasedSchedulingGroups_groups.sizeRange(ls, otherOffset, from, to)
    }
}

case class TTC_11_24_EnableTimeBasedSchedulingGroups (
    groups: TTC_11_24_EnableTimeBasedSchedulingGroups_groups
) {

    def size(offset: Long): Long = {
        require((0L <= offset) && (offset <= 9223372036854644735L))
        val size_0 = 32L
        val size_1 = this.groups.size(offset + size_0)
        size_0 + size_1
    }.ensuring { (res: Long) => 
        (0L <= res) && (res <= 131072L)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaAnyOffset(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854644735L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854644735L))
        val size_0_offset = 32L
        val size_0_otherOffset = 32L
        val size_1_offset = this.groups.size(offset + size_0_offset)
        val size_1_otherOffset = this.groups.size(otherOffset + size_0_otherOffset)
        this.groups.sizeLemmaAnyOffset(offset + size_0_offset, otherOffset + size_0_otherOffset)
        ()
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextByte(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854644735L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854644735L))
        require((offset % 8L) == (otherOffset % 8L))
        val size_0_offset = 32L
        val size_0_otherOffset = 32L
        val size_1_offset = this.groups.size(offset + size_0_offset)
        val size_1_otherOffset = this.groups.size(otherOffset + size_0_otherOffset)
        this.groups.sizeLemmaNextByte(offset + size_0_offset, otherOffset + size_0_otherOffset)
        ()
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextWord(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854644735L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854644735L))
        require((offset % 16L) == (otherOffset % 16L))
        val size_0_offset = 32L
        val size_0_otherOffset = 32L
        val size_1_offset = this.groups.size(offset + size_0_offset)
        val size_1_otherOffset = this.groups.size(otherOffset + size_0_otherOffset)
        this.groups.sizeLemmaNextWord(offset + size_0_offset, otherOffset + size_0_otherOffset)
        ()
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextDWord(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854644735L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854644735L))
        require((offset % 32L) == (otherOffset % 32L))
        val size_0_offset = 32L
        val size_0_otherOffset = 32L
        val size_1_offset = this.groups.size(offset + size_0_offset)
        val size_1_otherOffset = this.groups.size(otherOffset + size_0_otherOffset)
        this.groups.sizeLemmaNextDWord(offset + size_0_offset, otherOffset + size_0_otherOffset)
        ()
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }
}


@inline @cCode.inline val ERR_TC_11_24_ENABLETIMEBASEDSCHEDULINGGROUPS_GROUPS: Int = 1196 /*(SIZE(0 .. maxTC-11-24-GroupCount)) */
@inline @cCode.inline val ERR_TC_11_24_ENABLETIMEBASEDSCHEDULINGGROUPS_GROUPS_ELM_2: Int = 1191 /* */

@inline @cCode.inline val ERR_TC_11_24_ENABLETIMEBASEDSCHEDULINGGROUPS: Int = 1201 /* */

 
 

@inline @cCode.inline val ERR_ACN_ENCODE_TC_11_24_ENABLETIMEBASEDSCHEDULINGGROUPS: Int = 1204 /*  */
@inline @cCode.inline val ERR_ACN_ENCODE_TC_11_24_ENABLETIMEBASEDSCHEDULINGGROUPS_N_UNINITIALIZED: Int = 1205 /*  */
@inline @cCode.inline val ERR_ACN_ENCODE_TC_11_24_ENABLETIMEBASEDSCHEDULINGGROUPS_N: Int = 1184 /*  */
@inline @cCode.inline val ERR_ACN_ENCODE_TC_11_24_ENABLETIMEBASEDSCHEDULINGGROUPS_GROUPS: Int = 1199 /*  */
@inline @cCode.inline val ERR_ACN_ENCODE_TC_11_24_ENABLETIMEBASEDSCHEDULINGGROUPS_GROUPS_ELM_2: Int = 1194 /*  */
@inline @cCode.inline val TTC_11_24_EnableTimeBasedSchedulingGroups_REQUIRED_BYTES_FOR_ACN_ENCODING = 16384
@inline @cCode.inline val TTC_11_24_EnableTimeBasedSchedulingGroups_REQUIRED_BITS_FOR_ACN_ENCODING = 131072

@inline @cCode.inline val ERR_ACN_DECODE_TC_11_24_ENABLETIMEBASEDSCHEDULINGGROUPS: Int = 1206 /*  */
@inline @cCode.inline val ERR_ACN_DECODE_TC_11_24_ENABLETIMEBASEDSCHEDULINGGROUPS_N: Int = 1185 /*  */
@inline @cCode.inline val ERR_ACN_DECODE_TC_11_24_ENABLETIMEBASEDSCHEDULINGGROUPS_GROUPS: Int = 1200 /*  */
@inline @cCode.inline val ERR_ACN_DECODE_TC_11_24_ENABLETIMEBASEDSCHEDULINGGROUPS_GROUPS_ELM_2: Int = 1195 /*  */

// @inline @cCode.inline val maxTC_11_24_GroupCount: Int = 4095 // defined in body

