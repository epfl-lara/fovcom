/*
Code automatically generated by asn1scc tool
*/
package asn1src

import asn1scala._
import stainless.lang._
import stainless.annotation._
import stainless.collection._
import stainless.proof._
import StaticChecks._

/*-- TTC_11_21_DisableTimeBasedSubSchedules --------------------------------------------*/


case class TTC_11_21_DisableTimeBasedSubSchedules_subschedules (nCount: Int, arr: Vector[TSubSchedule_ID])
{
    require(this.arr.size <= 4095 && 0 <= this.nCount && this.nCount <= this.arr.size)

    def size(offset: Long): Long = {
        require((0L <= offset) && (offset <= 9223372036854644767L))
        TTC_11_21_DisableTimeBasedSubSchedules_subschedules.sizeRange(this.arr, offset, 0, this.nCount)
    }.ensuring { (res: Long) => 
        (0L <= res) && (res <= 131040L)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaAnyOffset(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854644767L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854644767L))
        TTC_11_21_DisableTimeBasedSubSchedules_subschedules.sizeLemmaAnyOffset(this.arr, offset, otherOffset, 0, this.nCount)
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextByte(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854644767L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854644767L))
        require((offset % 8L) == (otherOffset % 8L))
        TTC_11_21_DisableTimeBasedSubSchedules_subschedules.sizeLemmaNextByte(this.arr, offset, otherOffset, 0, this.nCount)
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextWord(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854644767L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854644767L))
        require((offset % 16L) == (otherOffset % 16L))
        TTC_11_21_DisableTimeBasedSubSchedules_subschedules.sizeLemmaNextWord(this.arr, offset, otherOffset, 0, this.nCount)
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextDWord(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854644767L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854644767L))
        require((offset % 32L) == (otherOffset % 32L))
        TTC_11_21_DisableTimeBasedSubSchedules_subschedules.sizeLemmaNextDWord(this.arr, offset, otherOffset, 0, this.nCount)
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }
}
object TTC_11_21_DisableTimeBasedSubSchedules_subschedules {
    def sizeRange(ls: Vector[ULong], offset: Long, from: Int, to: Int): Long = {
        require((0 <= from) && (from <= to) && (to <= ls.size) && (ls.size <= 4095))
        require((0L <= offset) && (offset <= 9223372036854775807L - 32L * (to - from)))
        decreases(to - from)
        if (from == to) {
            0L
        } else {
            val elemSize = 32L
            assert(elemSize == 32L)
            elemSize + TTC_11_21_DisableTimeBasedSubSchedules_subschedules.sizeRange(ls, offset + elemSize, from + 1, to)
        }
    }.ensuring { (res: Long) => 
        val nbElems = (to - from)
        (0L <= res && res <= 32L * nbElems)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaAnyOffset(ls: Vector[ULong], offset: Long, otherOffset: Long, from: Int, to: Int): Unit = {
        require((0 <= from) && (from <= to) && (to <= ls.size) && (ls.size <= 4095))
        require((0L <= offset) && (offset <= 9223372036854775807L - 32L * (to - from)))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854775807L - 32L * (to - from)))
        decreases(to - from)
        if (from == to) {
            ()
        } else {
            val elemSizeOff = 32L
            val elemSizeOtherOff = 32L
            assert(elemSizeOff == 32L)
            assert(elemSizeOtherOff == 32L)
            TTC_11_21_DisableTimeBasedSubSchedules_subschedules.sizeLemmaAnyOffset(ls, offset + elemSizeOff, otherOffset + elemSizeOtherOff, from + 1, to)
        }
    }.ensuring { (_: Unit) => 
        TTC_11_21_DisableTimeBasedSubSchedules_subschedules.sizeRange(ls, offset, from, to) == TTC_11_21_DisableTimeBasedSubSchedules_subschedules.sizeRange(ls, otherOffset, from, to)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextByte(ls: Vector[ULong], offset: Long, otherOffset: Long, from: Int, to: Int): Unit = {
        require((0 <= from) && (from <= to) && (to <= ls.size) && (ls.size <= 4095))
        require((0L <= offset) && (offset <= 9223372036854775807L - 32L * (to - from)))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854775807L - 32L * (to - from)))
        require((offset % 8L) == (otherOffset % 8L))
        decreases(to - from)
        if (from == to) {
            ()
        } else {
            val elemSizeOff = 32L
            val elemSizeOtherOff = 32L
            assert(elemSizeOff == 32L)
            assert(elemSizeOtherOff == 32L)
            TTC_11_21_DisableTimeBasedSubSchedules_subschedules.sizeLemmaNextByte(ls, offset + elemSizeOff, otherOffset + elemSizeOtherOff, from + 1, to)
        }
    }.ensuring { (_: Unit) => 
        TTC_11_21_DisableTimeBasedSubSchedules_subschedules.sizeRange(ls, offset, from, to) == TTC_11_21_DisableTimeBasedSubSchedules_subschedules.sizeRange(ls, otherOffset, from, to)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextWord(ls: Vector[ULong], offset: Long, otherOffset: Long, from: Int, to: Int): Unit = {
        require((0 <= from) && (from <= to) && (to <= ls.size) && (ls.size <= 4095))
        require((0L <= offset) && (offset <= 9223372036854775807L - 32L * (to - from)))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854775807L - 32L * (to - from)))
        require((offset % 16L) == (otherOffset % 16L))
        decreases(to - from)
        if (from == to) {
            ()
        } else {
            val elemSizeOff = 32L
            val elemSizeOtherOff = 32L
            assert(elemSizeOff == 32L)
            assert(elemSizeOtherOff == 32L)
            TTC_11_21_DisableTimeBasedSubSchedules_subschedules.sizeLemmaNextWord(ls, offset + elemSizeOff, otherOffset + elemSizeOtherOff, from + 1, to)
        }
    }.ensuring { (_: Unit) => 
        TTC_11_21_DisableTimeBasedSubSchedules_subschedules.sizeRange(ls, offset, from, to) == TTC_11_21_DisableTimeBasedSubSchedules_subschedules.sizeRange(ls, otherOffset, from, to)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextDWord(ls: Vector[ULong], offset: Long, otherOffset: Long, from: Int, to: Int): Unit = {
        require((0 <= from) && (from <= to) && (to <= ls.size) && (ls.size <= 4095))
        require((0L <= offset) && (offset <= 9223372036854775807L - 32L * (to - from)))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854775807L - 32L * (to - from)))
        require((offset % 32L) == (otherOffset % 32L))
        decreases(to - from)
        if (from == to) {
            ()
        } else {
            val elemSizeOff = 32L
            val elemSizeOtherOff = 32L
            assert(elemSizeOff == 32L)
            assert(elemSizeOtherOff == 32L)
            TTC_11_21_DisableTimeBasedSubSchedules_subschedules.sizeLemmaNextDWord(ls, offset + elemSizeOff, otherOffset + elemSizeOtherOff, from + 1, to)
        }
    }.ensuring { (_: Unit) => 
        TTC_11_21_DisableTimeBasedSubSchedules_subschedules.sizeRange(ls, offset, from, to) == TTC_11_21_DisableTimeBasedSubSchedules_subschedules.sizeRange(ls, otherOffset, from, to)
    }
}

case class TTC_11_21_DisableTimeBasedSubSchedules (
    subschedules: TTC_11_21_DisableTimeBasedSubSchedules_subschedules
) {

    def size(offset: Long): Long = {
        require((0L <= offset) && (offset <= 9223372036854644735L))
        val size_0 = 32L
        val size_1 = this.subschedules.size(offset + size_0)
        size_0 + size_1
    }.ensuring { (res: Long) => 
        (0L <= res) && (res <= 131072L)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaAnyOffset(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854644735L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854644735L))
        val size_0_offset = 32L
        val size_0_otherOffset = 32L
        val size_1_offset = this.subschedules.size(offset + size_0_offset)
        val size_1_otherOffset = this.subschedules.size(otherOffset + size_0_otherOffset)
        this.subschedules.sizeLemmaAnyOffset(offset + size_0_offset, otherOffset + size_0_otherOffset)
        ()
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextByte(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854644735L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854644735L))
        require((offset % 8L) == (otherOffset % 8L))
        val size_0_offset = 32L
        val size_0_otherOffset = 32L
        val size_1_offset = this.subschedules.size(offset + size_0_offset)
        val size_1_otherOffset = this.subschedules.size(otherOffset + size_0_otherOffset)
        this.subschedules.sizeLemmaNextByte(offset + size_0_offset, otherOffset + size_0_otherOffset)
        ()
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextWord(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854644735L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854644735L))
        require((offset % 16L) == (otherOffset % 16L))
        val size_0_offset = 32L
        val size_0_otherOffset = 32L
        val size_1_offset = this.subschedules.size(offset + size_0_offset)
        val size_1_otherOffset = this.subschedules.size(otherOffset + size_0_otherOffset)
        this.subschedules.sizeLemmaNextWord(offset + size_0_offset, otherOffset + size_0_otherOffset)
        ()
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextDWord(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854644735L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854644735L))
        require((offset % 32L) == (otherOffset % 32L))
        val size_0_offset = 32L
        val size_0_otherOffset = 32L
        val size_1_offset = this.subschedules.size(offset + size_0_offset)
        val size_1_otherOffset = this.subschedules.size(otherOffset + size_0_otherOffset)
        this.subschedules.sizeLemmaNextDWord(offset + size_0_offset, otherOffset + size_0_otherOffset)
        ()
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }
}


@inline @cCode.inline val ERR_TC_11_21_DISABLETIMEBASEDSUBSCHEDULES_SUBSCHEDULES: Int = 1082 /*(SIZE(0 .. maxTC-11-21-SubScheduleCount )) */
@inline @cCode.inline val ERR_TC_11_21_DISABLETIMEBASEDSUBSCHEDULES_SUBSCHEDULES_ELM_2: Int = 1077 /* */

@inline @cCode.inline val ERR_TC_11_21_DISABLETIMEBASEDSUBSCHEDULES: Int = 1087 /* */

 
 

@inline @cCode.inline val ERR_ACN_ENCODE_TC_11_21_DISABLETIMEBASEDSUBSCHEDULES: Int = 1090 /*  */
@inline @cCode.inline val ERR_ACN_ENCODE_TC_11_21_DISABLETIMEBASEDSUBSCHEDULES_N_UNINITIALIZED: Int = 1091 /*  */
@inline @cCode.inline val ERR_ACN_ENCODE_TC_11_21_DISABLETIMEBASEDSUBSCHEDULES_N: Int = 1070 /*  */
@inline @cCode.inline val ERR_ACN_ENCODE_TC_11_21_DISABLETIMEBASEDSUBSCHEDULES_SUBSCHEDULES: Int = 1085 /*  */
@inline @cCode.inline val ERR_ACN_ENCODE_TC_11_21_DISABLETIMEBASEDSUBSCHEDULES_SUBSCHEDULES_ELM_2: Int = 1080 /*  */
@inline @cCode.inline val TTC_11_21_DisableTimeBasedSubSchedules_REQUIRED_BYTES_FOR_ACN_ENCODING = 16384
@inline @cCode.inline val TTC_11_21_DisableTimeBasedSubSchedules_REQUIRED_BITS_FOR_ACN_ENCODING = 131072

@inline @cCode.inline val ERR_ACN_DECODE_TC_11_21_DISABLETIMEBASEDSUBSCHEDULES: Int = 1092 /*  */
@inline @cCode.inline val ERR_ACN_DECODE_TC_11_21_DISABLETIMEBASEDSUBSCHEDULES_N: Int = 1071 /*  */
@inline @cCode.inline val ERR_ACN_DECODE_TC_11_21_DISABLETIMEBASEDSUBSCHEDULES_SUBSCHEDULES: Int = 1086 /*  */
@inline @cCode.inline val ERR_ACN_DECODE_TC_11_21_DISABLETIMEBASEDSUBSCHEDULES_SUBSCHEDULES_ELM_2: Int = 1081 /*  */

// @inline @cCode.inline val maxTC_11_21_SubScheduleCount: Int = 4095 // defined in body

