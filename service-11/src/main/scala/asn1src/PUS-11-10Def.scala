/*
Code automatically generated by asn1scc tool
*/
package asn1src

import asn1scala._
import stainless.lang._
import stainless.annotation._
import stainless.collection._
import stainless.proof._
import StaticChecks._

/*-- TTM_11_10_Report --------------------------------------------*/

case class TTM_11_10_Report (
    subschedule_ID: OptionMut[TSubSchedule_ID], 
    group_ID: OptionMut[TGroup_ID], 
    releaseTime: TCDS_AbsoluteTime_PFC_2, 
    request: TDummyType
) {
    require(this.subschedule_ID.isDefined && this.group_ID.isDefined)

    def size(offset: Long): Long = {
        require((0L <= offset) && (offset <= 9223372036854775675L))
        val size_0 = (this.subschedule_ID match {
            case SomeMut(v) =>
                32L
            case NoneMut() =>
                0L
        })
        val size_1 = this.group_ID match {
            case SomeMut(v) =>
                32L
            case NoneMut() =>
                0L
        }
        val size_2 = this.releaseTime.size(offset + size_0 + size_1)
        val size_3 = 4L
        size_0 + size_1 + size_2 + size_3
    }.ensuring { (res: Long) => 
        res == 132L
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaAnyOffset(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854775675L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854775675L))
        val size_0_offset = (this.subschedule_ID match {
            case SomeMut(v) =>
                32L
            case NoneMut() =>
                0L
        })
        val size_0_otherOffset = this.subschedule_ID match {
            case SomeMut(v) =>
                32L
            case NoneMut() =>
                0L
        }
        val size_1_offset = this.group_ID match {
            case SomeMut(v) =>
                32L
            case NoneMut() =>
                0L
        }
        val size_1_otherOffset = this.group_ID match {
            case SomeMut(v) =>
                32L
            case NoneMut() =>
                0L
        }
        val size_2_offset = this.releaseTime.size(offset + size_0_offset + size_1_offset)
        val size_2_otherOffset = this.releaseTime.size(otherOffset + size_0_otherOffset + size_1_otherOffset)
        this.releaseTime.sizeLemmaAnyOffset(offset + size_0_offset + size_1_offset, otherOffset + size_0_otherOffset + size_1_otherOffset)
        val size_3_offset = 4L
        val size_3_otherOffset = 4L
        ()
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextByte(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854775675L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854775675L))
        require((offset % 8L) == (otherOffset % 8L))
        val size_0_offset = (this.subschedule_ID match {
            case SomeMut(v) =>
                32L
            case NoneMut() =>
                0L
        })
        val size_0_otherOffset = this.subschedule_ID match {
            case SomeMut(v) =>
                32L
            case NoneMut() =>
                0L
        }
        val size_1_offset = this.group_ID match {
            case SomeMut(v) =>
                32L
            case NoneMut() =>
                0L
        }
        val size_1_otherOffset = this.group_ID match {
            case SomeMut(v) =>
                32L
            case NoneMut() =>
                0L
        }
        val size_2_offset = this.releaseTime.size(offset + size_0_offset + size_1_offset)
        val size_2_otherOffset = this.releaseTime.size(otherOffset + size_0_otherOffset + size_1_otherOffset)
        this.releaseTime.sizeLemmaNextByte(offset + size_0_offset + size_1_offset, otherOffset + size_0_otherOffset + size_1_otherOffset)
        val size_3_offset = 4L
        val size_3_otherOffset = 4L
        ()
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextWord(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854775675L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854775675L))
        require((offset % 16L) == (otherOffset % 16L))
        val size_0_offset = (this.subschedule_ID match {
            case SomeMut(v) =>
                32L
            case NoneMut() =>
                0L
        })
        val size_0_otherOffset = this.subschedule_ID match {
            case SomeMut(v) =>
                32L
            case NoneMut() =>
                0L
        }
        val size_1_offset = this.group_ID match {
            case SomeMut(v) =>
                32L
            case NoneMut() =>
                0L
        }
        val size_1_otherOffset = this.group_ID match {
            case SomeMut(v) =>
                32L
            case NoneMut() =>
                0L
        }
        val size_2_offset = this.releaseTime.size(offset + size_0_offset + size_1_offset)
        val size_2_otherOffset = this.releaseTime.size(otherOffset + size_0_otherOffset + size_1_otherOffset)
        this.releaseTime.sizeLemmaNextWord(offset + size_0_offset + size_1_offset, otherOffset + size_0_otherOffset + size_1_otherOffset)
        val size_3_offset = 4L
        val size_3_otherOffset = 4L
        ()
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextDWord(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854775675L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854775675L))
        require((offset % 32L) == (otherOffset % 32L))
        val size_0_offset = (this.subschedule_ID match {
            case SomeMut(v) =>
                32L
            case NoneMut() =>
                0L
        })
        val size_0_otherOffset = this.subschedule_ID match {
            case SomeMut(v) =>
                32L
            case NoneMut() =>
                0L
        }
        val size_1_offset = this.group_ID match {
            case SomeMut(v) =>
                32L
            case NoneMut() =>
                0L
        }
        val size_1_otherOffset = this.group_ID match {
            case SomeMut(v) =>
                32L
            case NoneMut() =>
                0L
        }
        val size_2_offset = this.releaseTime.size(offset + size_0_offset + size_1_offset)
        val size_2_otherOffset = this.releaseTime.size(otherOffset + size_0_otherOffset + size_1_otherOffset)
        this.releaseTime.sizeLemmaNextDWord(offset + size_0_offset + size_1_offset, otherOffset + size_0_otherOffset + size_1_otherOffset)
        val size_3_offset = 4L
        val size_3_otherOffset = 4L
        ()
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }
}


@inline @cCode.inline val ERR_TM_11_10_REPORT: Int = 154 /*(WITH COMPONENTS
                    {
                        subschedule-ID PRESENT,
                        group-ID PRESENT
                    }) */
@inline @cCode.inline val ERR_TM_11_10_REPORT_SUBSCHEDULE_ID_2: Int = 99 /* */
@inline @cCode.inline val ERR_TM_11_10_REPORT_GROUP_ID_2: Int = 109 /* */
@inline @cCode.inline val ERR_TM_11_10_REPORT_RELEASETIME_2_2: Int = 139 /* */
@inline @cCode.inline val ERR_TM_11_10_REPORT_REQUEST_2: Int = 149 /* */

 

@inline @cCode.inline val ERR_ACN_ENCODE_TM_11_10_REPORT: Int = 157 /*  */
@inline @cCode.inline val ERR_ACN_ENCODE_TM_11_10_REPORT_SUBSCHEDULE_ID_2: Int = 102 /*  */
@inline @cCode.inline val ERR_ACN_ENCODE_TM_11_10_REPORT_GROUP_ID_2: Int = 112 /*  */
@inline @cCode.inline val ERR_ACN_ENCODE_TM_11_10_REPORT_RELEASETIME_2_2: Int = 142 /*  */
@inline @cCode.inline val ERR_ACN_ENCODE_TM_11_10_REPORT_REQUEST_2: Int = 152 /*  */
@inline @cCode.inline val TTM_11_10_Report_REQUIRED_BYTES_FOR_ACN_ENCODING = 17
@inline @cCode.inline val TTM_11_10_Report_REQUIRED_BITS_FOR_ACN_ENCODING = 132

@inline @cCode.inline val ERR_ACN_DECODE_TM_11_10_REPORT: Int = 158 /*  */
@inline @cCode.inline val ERR_ACN_DECODE_TM_11_10_REPORT_SUBSCHEDULE_ID_2: Int = 103 /*  */
@inline @cCode.inline val ERR_ACN_DECODE_TM_11_10_REPORT_GROUP_ID_2: Int = 113 /*  */
@inline @cCode.inline val ERR_ACN_DECODE_TM_11_10_REPORT_RELEASETIME_2_2: Int = 143 /*  */
@inline @cCode.inline val ERR_ACN_DECODE_TM_11_10_REPORT_REQUEST_2: Int = 153 /*  */
/*-- TTM_11_10_TimeBasedScheduleDetailReport --------------------------------------------*/


case class TTM_11_10_TimeBasedScheduleDetailReport_report (nCount: Int, arr: Vector[TTM_11_10_Report])
{
    require(this.arr.size <= 4095 && 1 <= this.nCount && this.nCount <= this.arr.size)

    def size(offset: Long): Long = {
        require((0L <= offset) && (offset <= 9223372036854235267L))
        TTM_11_10_TimeBasedScheduleDetailReport_report.sizeRange(this.arr, offset, 0, this.nCount)
    }.ensuring { (res: Long) => 
        (0L <= res) && (res <= 540540L)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaAnyOffset(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854235267L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854235267L))
        TTM_11_10_TimeBasedScheduleDetailReport_report.sizeLemmaAnyOffset(this.arr, offset, otherOffset, 0, this.nCount)
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextByte(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854235267L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854235267L))
        require((offset % 8L) == (otherOffset % 8L))
        TTM_11_10_TimeBasedScheduleDetailReport_report.sizeLemmaNextByte(this.arr, offset, otherOffset, 0, this.nCount)
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextWord(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854235267L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854235267L))
        require((offset % 16L) == (otherOffset % 16L))
        TTM_11_10_TimeBasedScheduleDetailReport_report.sizeLemmaNextWord(this.arr, offset, otherOffset, 0, this.nCount)
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextDWord(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854235267L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854235267L))
        require((offset % 32L) == (otherOffset % 32L))
        TTM_11_10_TimeBasedScheduleDetailReport_report.sizeLemmaNextDWord(this.arr, offset, otherOffset, 0, this.nCount)
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }
}
object TTM_11_10_TimeBasedScheduleDetailReport_report {
    def sizeRange(ls: Vector[TTM_11_10_Report], offset: Long, from: Int, to: Int): Long = {
        require((0 <= from) && (from <= to) && (to <= ls.size) && (ls.size <= 4095))
        require((0L <= offset) && (offset <= 9223372036854775807L - 132L * (to - from)))
        decreases(to - from)
        if (from == to) {
            0L
        } else {
            val elemSize = ls.apply(from).size(offset)
            assert(elemSize == 132L)
            elemSize + TTM_11_10_TimeBasedScheduleDetailReport_report.sizeRange(ls, offset + elemSize, from + 1, to)
        }
    }.ensuring { (res: Long) => 
        val nbElems = (to - from)
        (0L <= res && res <= 132L * nbElems)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaAnyOffset(ls: Vector[TTM_11_10_Report], offset: Long, otherOffset: Long, from: Int, to: Int): Unit = {
        require((0 <= from) && (from <= to) && (to <= ls.size) && (ls.size <= 4095))
        require((0L <= offset) && (offset <= 9223372036854775807L - 132L * (to - from)))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854775807L - 132L * (to - from)))
        decreases(to - from)
        if (from == to) {
            ()
        } else {
            val elemSizeOff = ls.apply(from).size(offset)
            val elemSizeOtherOff = ls.apply(from).size(otherOffset)
            assert(elemSizeOff == 132L)
            assert(elemSizeOtherOff == 132L)
            ls.apply(from).sizeLemmaAnyOffset(offset, otherOffset)
            TTM_11_10_TimeBasedScheduleDetailReport_report.sizeLemmaAnyOffset(ls, offset + elemSizeOff, otherOffset + elemSizeOtherOff, from + 1, to)
        }
    }.ensuring { (_: Unit) => 
        TTM_11_10_TimeBasedScheduleDetailReport_report.sizeRange(ls, offset, from, to) == TTM_11_10_TimeBasedScheduleDetailReport_report.sizeRange(ls, otherOffset, from, to)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextByte(ls: Vector[TTM_11_10_Report], offset: Long, otherOffset: Long, from: Int, to: Int): Unit = {
        require((0 <= from) && (from <= to) && (to <= ls.size) && (ls.size <= 4095))
        require((0L <= offset) && (offset <= 9223372036854775807L - 132L * (to - from)))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854775807L - 132L * (to - from)))
        require((offset % 8L) == (otherOffset % 8L))
        decreases(to - from)
        if (from == to) {
            ()
        } else {
            val elemSizeOff = ls.apply(from).size(offset)
            val elemSizeOtherOff = ls.apply(from).size(otherOffset)
            assert(elemSizeOff == 132L)
            assert(elemSizeOtherOff == 132L)
            ls.apply(from).sizeLemmaNextByte(offset, otherOffset)
            TTM_11_10_TimeBasedScheduleDetailReport_report.sizeLemmaNextByte(ls, offset + elemSizeOff, otherOffset + elemSizeOtherOff, from + 1, to)
        }
    }.ensuring { (_: Unit) => 
        TTM_11_10_TimeBasedScheduleDetailReport_report.sizeRange(ls, offset, from, to) == TTM_11_10_TimeBasedScheduleDetailReport_report.sizeRange(ls, otherOffset, from, to)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextWord(ls: Vector[TTM_11_10_Report], offset: Long, otherOffset: Long, from: Int, to: Int): Unit = {
        require((0 <= from) && (from <= to) && (to <= ls.size) && (ls.size <= 4095))
        require((0L <= offset) && (offset <= 9223372036854775807L - 132L * (to - from)))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854775807L - 132L * (to - from)))
        require((offset % 16L) == (otherOffset % 16L))
        decreases(to - from)
        if (from == to) {
            ()
        } else {
            val elemSizeOff = ls.apply(from).size(offset)
            val elemSizeOtherOff = ls.apply(from).size(otherOffset)
            assert(elemSizeOff == 132L)
            assert(elemSizeOtherOff == 132L)
            ls.apply(from).sizeLemmaNextWord(offset, otherOffset)
            TTM_11_10_TimeBasedScheduleDetailReport_report.sizeLemmaNextWord(ls, offset + elemSizeOff, otherOffset + elemSizeOtherOff, from + 1, to)
        }
    }.ensuring { (_: Unit) => 
        TTM_11_10_TimeBasedScheduleDetailReport_report.sizeRange(ls, offset, from, to) == TTM_11_10_TimeBasedScheduleDetailReport_report.sizeRange(ls, otherOffset, from, to)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextDWord(ls: Vector[TTM_11_10_Report], offset: Long, otherOffset: Long, from: Int, to: Int): Unit = {
        require((0 <= from) && (from <= to) && (to <= ls.size) && (ls.size <= 4095))
        require((0L <= offset) && (offset <= 9223372036854775807L - 132L * (to - from)))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854775807L - 132L * (to - from)))
        require((offset % 32L) == (otherOffset % 32L))
        decreases(to - from)
        if (from == to) {
            ()
        } else {
            val elemSizeOff = ls.apply(from).size(offset)
            val elemSizeOtherOff = ls.apply(from).size(otherOffset)
            assert(elemSizeOff == 132L)
            assert(elemSizeOtherOff == 132L)
            ls.apply(from).sizeLemmaNextDWord(offset, otherOffset)
            TTM_11_10_TimeBasedScheduleDetailReport_report.sizeLemmaNextDWord(ls, offset + elemSizeOff, otherOffset + elemSizeOtherOff, from + 1, to)
        }
    }.ensuring { (_: Unit) => 
        TTM_11_10_TimeBasedScheduleDetailReport_report.sizeRange(ls, offset, from, to) == TTM_11_10_TimeBasedScheduleDetailReport_report.sizeRange(ls, otherOffset, from, to)
    }
}

case class TTM_11_10_TimeBasedScheduleDetailReport (
    report: TTM_11_10_TimeBasedScheduleDetailReport_report
) {

    def size(offset: Long): Long = {
        require((0L <= offset) && (offset <= 9223372036854235235L))
        val size_0 = 32L
        val size_1 = this.report.size(offset + size_0)
        size_0 + size_1
    }.ensuring { (res: Long) => 
        (0L <= res) && (res <= 540572L)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaAnyOffset(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854235235L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854235235L))
        val size_0_offset = 32L
        val size_0_otherOffset = 32L
        val size_1_offset = this.report.size(offset + size_0_offset)
        val size_1_otherOffset = this.report.size(otherOffset + size_0_otherOffset)
        this.report.sizeLemmaAnyOffset(offset + size_0_offset, otherOffset + size_0_otherOffset)
        ()
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextByte(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854235235L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854235235L))
        require((offset % 8L) == (otherOffset % 8L))
        val size_0_offset = 32L
        val size_0_otherOffset = 32L
        val size_1_offset = this.report.size(offset + size_0_offset)
        val size_1_otherOffset = this.report.size(otherOffset + size_0_otherOffset)
        this.report.sizeLemmaNextByte(offset + size_0_offset, otherOffset + size_0_otherOffset)
        ()
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextWord(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854235235L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854235235L))
        require((offset % 16L) == (otherOffset % 16L))
        val size_0_offset = 32L
        val size_0_otherOffset = 32L
        val size_1_offset = this.report.size(offset + size_0_offset)
        val size_1_otherOffset = this.report.size(otherOffset + size_0_otherOffset)
        this.report.sizeLemmaNextWord(offset + size_0_offset, otherOffset + size_0_otherOffset)
        ()
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextDWord(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854235235L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854235235L))
        require((offset % 32L) == (otherOffset % 32L))
        val size_0_offset = 32L
        val size_0_otherOffset = 32L
        val size_1_offset = this.report.size(offset + size_0_offset)
        val size_1_otherOffset = this.report.size(otherOffset + size_0_otherOffset)
        this.report.sizeLemmaNextDWord(offset + size_0_offset, otherOffset + size_0_otherOffset)
        ()
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }
}


@inline @cCode.inline val ERR_TM_11_10_TIMEBASEDSCHEDULEDETAILREPORT_REPORT: Int = 83 /*(SIZE(1 .. maxTM-11-10-ReportCount)) */
@inline @cCode.inline val ERR_TM_11_10_TIMEBASEDSCHEDULEDETAILREPORT_REPORT_ELM_2: Int = 78 /* */

@inline @cCode.inline val ERR_TM_11_10_TIMEBASEDSCHEDULEDETAILREPORT: Int = 88 /* */

 
 

@inline @cCode.inline val ERR_ACN_ENCODE_TM_11_10_TIMEBASEDSCHEDULEDETAILREPORT: Int = 91 /*  */
@inline @cCode.inline val ERR_ACN_ENCODE_TM_11_10_TIMEBASEDSCHEDULEDETAILREPORT_N_UNINITIALIZED: Int = 92 /*  */
@inline @cCode.inline val ERR_ACN_ENCODE_TM_11_10_TIMEBASEDSCHEDULEDETAILREPORT_N: Int = 11 /*  */
@inline @cCode.inline val ERR_ACN_ENCODE_TM_11_10_TIMEBASEDSCHEDULEDETAILREPORT_REPORT: Int = 86 /*  */
@inline @cCode.inline val ERR_ACN_ENCODE_TM_11_10_TIMEBASEDSCHEDULEDETAILREPORT_REPORT_ELM_2: Int = 81 /*  */
@inline @cCode.inline val TTM_11_10_TimeBasedScheduleDetailReport_REQUIRED_BYTES_FOR_ACN_ENCODING = 67572
@inline @cCode.inline val TTM_11_10_TimeBasedScheduleDetailReport_REQUIRED_BITS_FOR_ACN_ENCODING = 540572

@inline @cCode.inline val ERR_ACN_DECODE_TM_11_10_TIMEBASEDSCHEDULEDETAILREPORT: Int = 93 /*  */
@inline @cCode.inline val ERR_ACN_DECODE_TM_11_10_TIMEBASEDSCHEDULEDETAILREPORT_N: Int = 12 /*  */
@inline @cCode.inline val ERR_ACN_DECODE_TM_11_10_TIMEBASEDSCHEDULEDETAILREPORT_REPORT: Int = 87 /*  */
@inline @cCode.inline val ERR_ACN_DECODE_TM_11_10_TIMEBASEDSCHEDULEDETAILREPORT_REPORT_ELM_2: Int = 82 /*  */

// @inline @cCode.inline val maxTM_11_10_ReportCount: Int = 4095 // defined in body

