/*
Code automatically generated by asn1scc tool
*/
package asn1src

import asn1scala._
import stainless.lang.{ghost => ghostExpr, _}
import stainless.annotation._
import stainless.collection._
import stainless.proof._
import StaticChecks._


def TPeriodicGenerationActionStatus_IsConstraintValid(pVal: TPeriodicGenerationActionStatus): Either[ErrorCode, Int] =
{
    var ret: Either[ErrorCode, Int] = Right(0)
    ret = (((pVal == TPeriodicGenerationActionStatus.Tenabled)) || ((pVal == TPeriodicGenerationActionStatus.Tdisabled))) match
        case true =>
            Right(0)
        case false =>
            Left(45)
    ret
}

def TPeriodicGenerationActionStatus_Initialize(): TPeriodicGenerationActionStatus = TPeriodicGenerationActionStatus.Tenabled

@opaque @inlineOnce 
def TPeriodicGenerationActionStatus_ACN_Encode(pVal: TPeriodicGenerationActionStatus, codec: ACN, bCheckConstraints: Boolean): Either[ErrorCode, Int] =  // acn:21
{
    require(codec.base.bitStream.validate_offset_bits(1L))
    TPeriodicGenerationActionStatus_IsConstraintValid(pVal) match
        case Left(l) => return Left(l)
        case Right(_) =>
    @ghost val oldCdc = snapshot(codec)
    val intVal_pVal = pVal match
        case TPeriodicGenerationActionStatus.Tenabled => ULong.fromRaw(0L)
        case TPeriodicGenerationActionStatus.Tdisabled => ULong.fromRaw(1L)

    locally {
        ghostExpr {
            @opaque @inlineOnce
            def bitCountLemma(v: ULong): Unit = {
                require(v <= ULong.fromRaw(1))
            }.ensuring(_ => GetBitCountUnsigned(v) <= 1)
            bitCountLemma(intVal_pVal)
        }
    }
    codec.enc_Int_PositiveInteger_ConstSize(intVal_pVal, 1)
    Right(0)
}.ensuring { (res : Either[ErrorCode, Int]) =>
    res match {
        case Left(_) =>
            true
        case Right(_) =>
            old(codec).base.buf.length == codec.base.buf.length && codec.base.bitStream.bitIndex == old(codec).base.bitStream.bitIndex + 1L
    }
}

@opaque @inlineOnce 
def TPeriodicGenerationActionStatus_ACN_Decode(codec: ACN): EitherMut[ErrorCode, TPeriodicGenerationActionStatus] =
{
    require(codec.base.bitStream.validate_offset_bits(1L))

    @ghost val oldCdc = snapshot(codec)
    val intVal_pVal = codec.dec_Int_PositiveInteger_ConstSize(1)

    val pVal = intVal_pVal.toRaw match
        case 0 => TPeriodicGenerationActionStatus.Tenabled
        case 1 => TPeriodicGenerationActionStatus.Tdisabled
        case _ => return LeftMut(ERR_ACN_DECODE_PERIODICGENERATIONACTIONSTATUS)

    TPeriodicGenerationActionStatus_IsConstraintValid(pVal) match
        case Left(l) => LeftMut[ErrorCode, TPeriodicGenerationActionStatus](l)
        case Right(_) => RightMut[ErrorCode, TPeriodicGenerationActionStatus](pVal)
}.ensuring { (res : EitherMut[ErrorCode, TPeriodicGenerationActionStatus]) =>
    res match {
        case LeftMut(_) =>
            true
        case RightMut(resVal) =>
            old(codec).base.buf == codec.base.buf && codec.base.bitStream.bitIndex == old(codec).base.bitStream.bitIndex + 1L && TPeriodicGenerationActionStatus_IsConstraintValid(resVal).isRight
    }
}

@ghost @pure 
def TPeriodicGenerationActionStatus_ACN_Decode_pure(codec: ACN): (ACN, EitherMut[ErrorCode, TPeriodicGenerationActionStatus]) =
{
    require(codec.base.bitStream.validate_offset_bits(1L))
    val cpy = snapshot(codec)
    val res = TPeriodicGenerationActionStatus_ACN_Decode(cpy)
    (cpy, res)
}