/*
Code automatically generated by asn1scc tool
*/
package asn1src

import asn1scala._
import stainless.lang._
import stainless.annotation._
import stainless.collection._
import stainless.proof._
import StaticChecks._

sealed trait TDeviceAddress:
    def i: Int
object TDeviceAddress:
    case object Taddr0 extends TDeviceAddress:
        override def i: Int = 1

// please use the following macros to avoid breaking code.
// type Taddr0 = TDeviceAddress.addr0

@inline @cCode.inline val ERR_DEVICEADDRESS: Int = 361 /*addr0 */

 

@inline @cCode.inline val ERR_ACN_ENCODE_DEVICEADDRESS: Int = 364 /*  */
@inline @cCode.inline val TDeviceAddress_REQUIRED_BYTES_FOR_ACN_ENCODING = 4
@inline @cCode.inline val TDeviceAddress_REQUIRED_BITS_FOR_ACN_ENCODING = 32

@inline @cCode.inline val ERR_ACN_DECODE_DEVICEADDRESS: Int = 365 /*  */
/*-- TTC_2_1_DistributeOnOffDeviceCommands --------------------------------------------*/


case class TTC_2_1_DistributeOnOffDeviceCommands_onOffDeviceAddresses (nCount: Int, arr: Vector[TDeviceAddress])
{
    require(this.arr.size <= 63 && 1 <= this.nCount && this.nCount <= this.arr.size)

    def size(offset: Long): Long = {
        require((0L <= offset) && (offset <= 9223372036854773791L))
        TTC_2_1_DistributeOnOffDeviceCommands_onOffDeviceAddresses.sizeRange(this.arr, offset, 0, this.nCount)
    }.ensuring { (res: Long) => 
        (0L <= res) && (res <= 2016L)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaAnyOffset(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854773791L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854773791L))
        TTC_2_1_DistributeOnOffDeviceCommands_onOffDeviceAddresses.sizeLemmaAnyOffset(this.arr, offset, otherOffset, 0, this.nCount)
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextByte(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854773791L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854773791L))
        require((offset % 8L) == (otherOffset % 8L))
        TTC_2_1_DistributeOnOffDeviceCommands_onOffDeviceAddresses.sizeLemmaNextByte(this.arr, offset, otherOffset, 0, this.nCount)
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextWord(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854773791L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854773791L))
        require((offset % 16L) == (otherOffset % 16L))
        TTC_2_1_DistributeOnOffDeviceCommands_onOffDeviceAddresses.sizeLemmaNextWord(this.arr, offset, otherOffset, 0, this.nCount)
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextDWord(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854773791L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854773791L))
        require((offset % 32L) == (otherOffset % 32L))
        TTC_2_1_DistributeOnOffDeviceCommands_onOffDeviceAddresses.sizeLemmaNextDWord(this.arr, offset, otherOffset, 0, this.nCount)
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }
}
object TTC_2_1_DistributeOnOffDeviceCommands_onOffDeviceAddresses {
    def sizeRange(ls: Vector[TDeviceAddress], offset: Long, from: Int, to: Int): Long = {
        require((0 <= from) && (from <= to) && (to <= ls.size) && (ls.size <= 63))
        require((0L <= offset) && (offset <= 9223372036854775807L - 32L * (to - from)))
        decreases(to - from)
        if (from == to) {
            0L
        } else {
            val elemSize = 32L
            assert(elemSize == 32L)
            elemSize + TTC_2_1_DistributeOnOffDeviceCommands_onOffDeviceAddresses.sizeRange(ls, offset + elemSize, from + 1, to)
        }
    }.ensuring { (res: Long) => 
        val nbElems = (to - from)
        (0L <= res && res <= 32L * nbElems)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaAnyOffset(ls: Vector[TDeviceAddress], offset: Long, otherOffset: Long, from: Int, to: Int): Unit = {
        require((0 <= from) && (from <= to) && (to <= ls.size) && (ls.size <= 63))
        require((0L <= offset) && (offset <= 9223372036854775807L - 32L * (to - from)))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854775807L - 32L * (to - from)))
        decreases(to - from)
        if (from == to) {
            ()
        } else {
            val elemSizeOff = 32L
            val elemSizeOtherOff = 32L
            assert(elemSizeOff == 32L)
            assert(elemSizeOtherOff == 32L)
            TTC_2_1_DistributeOnOffDeviceCommands_onOffDeviceAddresses.sizeLemmaAnyOffset(ls, offset + elemSizeOff, otherOffset + elemSizeOtherOff, from + 1, to)
        }
    }.ensuring { (_: Unit) => 
        TTC_2_1_DistributeOnOffDeviceCommands_onOffDeviceAddresses.sizeRange(ls, offset, from, to) == TTC_2_1_DistributeOnOffDeviceCommands_onOffDeviceAddresses.sizeRange(ls, otherOffset, from, to)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextByte(ls: Vector[TDeviceAddress], offset: Long, otherOffset: Long, from: Int, to: Int): Unit = {
        require((0 <= from) && (from <= to) && (to <= ls.size) && (ls.size <= 63))
        require((0L <= offset) && (offset <= 9223372036854775807L - 32L * (to - from)))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854775807L - 32L * (to - from)))
        require((offset % 8L) == (otherOffset % 8L))
        decreases(to - from)
        if (from == to) {
            ()
        } else {
            val elemSizeOff = 32L
            val elemSizeOtherOff = 32L
            assert(elemSizeOff == 32L)
            assert(elemSizeOtherOff == 32L)
            TTC_2_1_DistributeOnOffDeviceCommands_onOffDeviceAddresses.sizeLemmaNextByte(ls, offset + elemSizeOff, otherOffset + elemSizeOtherOff, from + 1, to)
        }
    }.ensuring { (_: Unit) => 
        TTC_2_1_DistributeOnOffDeviceCommands_onOffDeviceAddresses.sizeRange(ls, offset, from, to) == TTC_2_1_DistributeOnOffDeviceCommands_onOffDeviceAddresses.sizeRange(ls, otherOffset, from, to)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextWord(ls: Vector[TDeviceAddress], offset: Long, otherOffset: Long, from: Int, to: Int): Unit = {
        require((0 <= from) && (from <= to) && (to <= ls.size) && (ls.size <= 63))
        require((0L <= offset) && (offset <= 9223372036854775807L - 32L * (to - from)))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854775807L - 32L * (to - from)))
        require((offset % 16L) == (otherOffset % 16L))
        decreases(to - from)
        if (from == to) {
            ()
        } else {
            val elemSizeOff = 32L
            val elemSizeOtherOff = 32L
            assert(elemSizeOff == 32L)
            assert(elemSizeOtherOff == 32L)
            TTC_2_1_DistributeOnOffDeviceCommands_onOffDeviceAddresses.sizeLemmaNextWord(ls, offset + elemSizeOff, otherOffset + elemSizeOtherOff, from + 1, to)
        }
    }.ensuring { (_: Unit) => 
        TTC_2_1_DistributeOnOffDeviceCommands_onOffDeviceAddresses.sizeRange(ls, offset, from, to) == TTC_2_1_DistributeOnOffDeviceCommands_onOffDeviceAddresses.sizeRange(ls, otherOffset, from, to)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextDWord(ls: Vector[TDeviceAddress], offset: Long, otherOffset: Long, from: Int, to: Int): Unit = {
        require((0 <= from) && (from <= to) && (to <= ls.size) && (ls.size <= 63))
        require((0L <= offset) && (offset <= 9223372036854775807L - 32L * (to - from)))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854775807L - 32L * (to - from)))
        require((offset % 32L) == (otherOffset % 32L))
        decreases(to - from)
        if (from == to) {
            ()
        } else {
            val elemSizeOff = 32L
            val elemSizeOtherOff = 32L
            assert(elemSizeOff == 32L)
            assert(elemSizeOtherOff == 32L)
            TTC_2_1_DistributeOnOffDeviceCommands_onOffDeviceAddresses.sizeLemmaNextDWord(ls, offset + elemSizeOff, otherOffset + elemSizeOtherOff, from + 1, to)
        }
    }.ensuring { (_: Unit) => 
        TTC_2_1_DistributeOnOffDeviceCommands_onOffDeviceAddresses.sizeRange(ls, offset, from, to) == TTC_2_1_DistributeOnOffDeviceCommands_onOffDeviceAddresses.sizeRange(ls, otherOffset, from, to)
    }
}

case class TTC_2_1_DistributeOnOffDeviceCommands (
    onOffDeviceAddresses: TTC_2_1_DistributeOnOffDeviceCommands_onOffDeviceAddresses
) {

    def size(offset: Long): Long = {
        require((0L <= offset) && (offset <= 9223372036854773759L))
        val size_0 = 32L
        val size_1 = this.onOffDeviceAddresses.size(offset + size_0)
        size_0 + size_1
    }.ensuring { (res: Long) => 
        (0L <= res) && (res <= 2048L)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaAnyOffset(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854773759L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854773759L))
        val size_0_offset = 32L
        val size_0_otherOffset = 32L
        val size_1_offset = this.onOffDeviceAddresses.size(offset + size_0_offset)
        val size_1_otherOffset = this.onOffDeviceAddresses.size(otherOffset + size_0_otherOffset)
        this.onOffDeviceAddresses.sizeLemmaAnyOffset(offset + size_0_offset, otherOffset + size_0_otherOffset)
        ()
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextByte(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854773759L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854773759L))
        require((offset % 8L) == (otherOffset % 8L))
        val size_0_offset = 32L
        val size_0_otherOffset = 32L
        val size_1_offset = this.onOffDeviceAddresses.size(offset + size_0_offset)
        val size_1_otherOffset = this.onOffDeviceAddresses.size(otherOffset + size_0_otherOffset)
        this.onOffDeviceAddresses.sizeLemmaNextByte(offset + size_0_offset, otherOffset + size_0_otherOffset)
        ()
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextWord(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854773759L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854773759L))
        require((offset % 16L) == (otherOffset % 16L))
        val size_0_offset = 32L
        val size_0_otherOffset = 32L
        val size_1_offset = this.onOffDeviceAddresses.size(offset + size_0_offset)
        val size_1_otherOffset = this.onOffDeviceAddresses.size(otherOffset + size_0_otherOffset)
        this.onOffDeviceAddresses.sizeLemmaNextWord(offset + size_0_offset, otherOffset + size_0_otherOffset)
        ()
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextDWord(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854773759L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854773759L))
        require((offset % 32L) == (otherOffset % 32L))
        val size_0_offset = 32L
        val size_0_otherOffset = 32L
        val size_1_offset = this.onOffDeviceAddresses.size(offset + size_0_offset)
        val size_1_otherOffset = this.onOffDeviceAddresses.size(otherOffset + size_0_otherOffset)
        this.onOffDeviceAddresses.sizeLemmaNextDWord(offset + size_0_offset, otherOffset + size_0_otherOffset)
        ()
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }
}


@inline @cCode.inline val ERR_TC_2_1_DISTRIBUTEONOFFDEVICECOMMANDS_ONOFFDEVICEADDRESSES: Int = 350 /*(SIZE(1 .. maxTC-2-1-OnOffDeviceAdressesCount)) */
@inline @cCode.inline val ERR_TC_2_1_DISTRIBUTEONOFFDEVICECOMMANDS_ONOFFDEVICEADDRESSES_ELM_2: Int = 345 /* */

@inline @cCode.inline val ERR_TC_2_1_DISTRIBUTEONOFFDEVICECOMMANDS: Int = 355 /* */

 
 

@inline @cCode.inline val ERR_ACN_ENCODE_TC_2_1_DISTRIBUTEONOFFDEVICECOMMANDS: Int = 358 /*  */
@inline @cCode.inline val ERR_ACN_ENCODE_TC_2_1_DISTRIBUTEONOFFDEVICECOMMANDS_N_UNINITIALIZED: Int = 359 /*  */
@inline @cCode.inline val ERR_ACN_ENCODE_TC_2_1_DISTRIBUTEONOFFDEVICECOMMANDS_N: Int = 338 /*  */
@inline @cCode.inline val ERR_ACN_ENCODE_TC_2_1_DISTRIBUTEONOFFDEVICECOMMANDS_ONOFFDEVICEADDRESSES: Int = 353 /*  */
@inline @cCode.inline val ERR_ACN_ENCODE_TC_2_1_DISTRIBUTEONOFFDEVICECOMMANDS_ONOFFDEVICEADDRESSES_ELM_2: Int = 348 /*  */
@inline @cCode.inline val TTC_2_1_DistributeOnOffDeviceCommands_REQUIRED_BYTES_FOR_ACN_ENCODING = 256
@inline @cCode.inline val TTC_2_1_DistributeOnOffDeviceCommands_REQUIRED_BITS_FOR_ACN_ENCODING = 2048

@inline @cCode.inline val ERR_ACN_DECODE_TC_2_1_DISTRIBUTEONOFFDEVICECOMMANDS: Int = 360 /*  */
@inline @cCode.inline val ERR_ACN_DECODE_TC_2_1_DISTRIBUTEONOFFDEVICECOMMANDS_N: Int = 339 /*  */
@inline @cCode.inline val ERR_ACN_DECODE_TC_2_1_DISTRIBUTEONOFFDEVICECOMMANDS_ONOFFDEVICEADDRESSES: Int = 354 /*  */
@inline @cCode.inline val ERR_ACN_DECODE_TC_2_1_DISTRIBUTEONOFFDEVICECOMMANDS_ONOFFDEVICEADDRESSES_ELM_2: Int = 349 /*  */

// @inline @cCode.inline val maxTC_2_1_OnOffDeviceAdressesCount: Int = 63 // defined in body

