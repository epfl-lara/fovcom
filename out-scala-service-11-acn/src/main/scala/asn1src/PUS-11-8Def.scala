/*
Code automatically generated by asn1scc tool
*/
package asn1src

import asn1scala._
import stainless.lang._
import stainless.annotation._
import stainless.collection._
import stainless.proof._
import StaticChecks._

/*-- TTC_11_8_Groups --------------------------------------------*/


case class TTC_11_8_Groups_subschedule_ID (nCount: Int, arr: Vector[TGroup_ID])
{
    require(this.arr.size <= 4095 && 0 <= this.nCount && this.nCount <= this.arr.size)

    def size(offset: Long): Long = {
        require((0L <= offset) && (offset <= 9223372036854644767L))
        TTC_11_8_Groups_subschedule_ID.sizeRange(this.arr, offset, 0, this.nCount)
    }.ensuring { (res: Long) => 
        (0L <= res) && (res <= 131040L)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaAnyOffset(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854644767L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854644767L))
        TTC_11_8_Groups_subschedule_ID.sizeLemmaAnyOffset(this.arr, offset, otherOffset, 0, this.nCount)
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextByte(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854644767L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854644767L))
        require((offset % 8L) == (otherOffset % 8L))
        TTC_11_8_Groups_subschedule_ID.sizeLemmaNextByte(this.arr, offset, otherOffset, 0, this.nCount)
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextWord(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854644767L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854644767L))
        require((offset % 16L) == (otherOffset % 16L))
        TTC_11_8_Groups_subschedule_ID.sizeLemmaNextWord(this.arr, offset, otherOffset, 0, this.nCount)
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextDWord(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854644767L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854644767L))
        require((offset % 32L) == (otherOffset % 32L))
        TTC_11_8_Groups_subschedule_ID.sizeLemmaNextDWord(this.arr, offset, otherOffset, 0, this.nCount)
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }
}
object TTC_11_8_Groups_subschedule_ID {
    def sizeRange(ls: Vector[ULong], offset: Long, from: Int, to: Int): Long = {
        require((0 <= from) && (from <= to) && (to <= ls.size) && (ls.size <= 4095))
        require((0L <= offset) && (offset <= 9223372036854775807L - 32L * (to - from)))
        decreases(to - from)
        if (from == to) {
            0L
        } else {
            val elemSize = 32L
            assert(elemSize == 32L)
            elemSize + TTC_11_8_Groups_subschedule_ID.sizeRange(ls, offset + elemSize, from + 1, to)
        }
    }.ensuring { (res: Long) => 
        val nbElems = (to - from)
        (0L <= res && res <= 32L * nbElems)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaAnyOffset(ls: Vector[ULong], offset: Long, otherOffset: Long, from: Int, to: Int): Unit = {
        require((0 <= from) && (from <= to) && (to <= ls.size) && (ls.size <= 4095))
        require((0L <= offset) && (offset <= 9223372036854775807L - 32L * (to - from)))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854775807L - 32L * (to - from)))
        decreases(to - from)
        if (from == to) {
            ()
        } else {
            val elemSizeOff = 32L
            val elemSizeOtherOff = 32L
            assert(elemSizeOff == 32L)
            assert(elemSizeOtherOff == 32L)
            TTC_11_8_Groups_subschedule_ID.sizeLemmaAnyOffset(ls, offset + elemSizeOff, otherOffset + elemSizeOtherOff, from + 1, to)
        }
    }.ensuring { (_: Unit) => 
        TTC_11_8_Groups_subschedule_ID.sizeRange(ls, offset, from, to) == TTC_11_8_Groups_subschedule_ID.sizeRange(ls, otherOffset, from, to)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextByte(ls: Vector[ULong], offset: Long, otherOffset: Long, from: Int, to: Int): Unit = {
        require((0 <= from) && (from <= to) && (to <= ls.size) && (ls.size <= 4095))
        require((0L <= offset) && (offset <= 9223372036854775807L - 32L * (to - from)))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854775807L - 32L * (to - from)))
        require((offset % 8L) == (otherOffset % 8L))
        decreases(to - from)
        if (from == to) {
            ()
        } else {
            val elemSizeOff = 32L
            val elemSizeOtherOff = 32L
            assert(elemSizeOff == 32L)
            assert(elemSizeOtherOff == 32L)
            TTC_11_8_Groups_subschedule_ID.sizeLemmaNextByte(ls, offset + elemSizeOff, otherOffset + elemSizeOtherOff, from + 1, to)
        }
    }.ensuring { (_: Unit) => 
        TTC_11_8_Groups_subschedule_ID.sizeRange(ls, offset, from, to) == TTC_11_8_Groups_subschedule_ID.sizeRange(ls, otherOffset, from, to)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextWord(ls: Vector[ULong], offset: Long, otherOffset: Long, from: Int, to: Int): Unit = {
        require((0 <= from) && (from <= to) && (to <= ls.size) && (ls.size <= 4095))
        require((0L <= offset) && (offset <= 9223372036854775807L - 32L * (to - from)))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854775807L - 32L * (to - from)))
        require((offset % 16L) == (otherOffset % 16L))
        decreases(to - from)
        if (from == to) {
            ()
        } else {
            val elemSizeOff = 32L
            val elemSizeOtherOff = 32L
            assert(elemSizeOff == 32L)
            assert(elemSizeOtherOff == 32L)
            TTC_11_8_Groups_subschedule_ID.sizeLemmaNextWord(ls, offset + elemSizeOff, otherOffset + elemSizeOtherOff, from + 1, to)
        }
    }.ensuring { (_: Unit) => 
        TTC_11_8_Groups_subschedule_ID.sizeRange(ls, offset, from, to) == TTC_11_8_Groups_subschedule_ID.sizeRange(ls, otherOffset, from, to)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextDWord(ls: Vector[ULong], offset: Long, otherOffset: Long, from: Int, to: Int): Unit = {
        require((0 <= from) && (from <= to) && (to <= ls.size) && (ls.size <= 4095))
        require((0L <= offset) && (offset <= 9223372036854775807L - 32L * (to - from)))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854775807L - 32L * (to - from)))
        require((offset % 32L) == (otherOffset % 32L))
        decreases(to - from)
        if (from == to) {
            ()
        } else {
            val elemSizeOff = 32L
            val elemSizeOtherOff = 32L
            assert(elemSizeOff == 32L)
            assert(elemSizeOtherOff == 32L)
            TTC_11_8_Groups_subschedule_ID.sizeLemmaNextDWord(ls, offset + elemSizeOff, otherOffset + elemSizeOtherOff, from + 1, to)
        }
    }.ensuring { (_: Unit) => 
        TTC_11_8_Groups_subschedule_ID.sizeRange(ls, offset, from, to) == TTC_11_8_Groups_subschedule_ID.sizeRange(ls, otherOffset, from, to)
    }
}

case class TTC_11_8_Groups (
    subschedule_ID: TTC_11_8_Groups_subschedule_ID
) {

    def size(offset: Long): Long = {
        require((0L <= offset) && (offset <= 9223372036854644735L))
        val size_0 = 32L
        val size_1 = this.subschedule_ID.size(offset + size_0)
        size_0 + size_1
    }.ensuring { (res: Long) => 
        (0L <= res) && (res <= 131072L)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaAnyOffset(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854644735L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854644735L))
        val size_0_offset = 32L
        val size_0_otherOffset = 32L
        val size_1_offset = this.subschedule_ID.size(offset + size_0_offset)
        val size_1_otherOffset = this.subschedule_ID.size(otherOffset + size_0_otherOffset)
        this.subschedule_ID.sizeLemmaAnyOffset(offset + size_0_offset, otherOffset + size_0_otherOffset)
        ()
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextByte(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854644735L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854644735L))
        require((offset % 8L) == (otherOffset % 8L))
        val size_0_offset = 32L
        val size_0_otherOffset = 32L
        val size_1_offset = this.subschedule_ID.size(offset + size_0_offset)
        val size_1_otherOffset = this.subschedule_ID.size(otherOffset + size_0_otherOffset)
        this.subschedule_ID.sizeLemmaNextByte(offset + size_0_offset, otherOffset + size_0_otherOffset)
        ()
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextWord(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854644735L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854644735L))
        require((offset % 16L) == (otherOffset % 16L))
        val size_0_offset = 32L
        val size_0_otherOffset = 32L
        val size_1_offset = this.subschedule_ID.size(offset + size_0_offset)
        val size_1_otherOffset = this.subschedule_ID.size(otherOffset + size_0_otherOffset)
        this.subschedule_ID.sizeLemmaNextWord(offset + size_0_offset, otherOffset + size_0_otherOffset)
        ()
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextDWord(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854644735L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854644735L))
        require((offset % 32L) == (otherOffset % 32L))
        val size_0_offset = 32L
        val size_0_otherOffset = 32L
        val size_1_offset = this.subschedule_ID.size(offset + size_0_offset)
        val size_1_otherOffset = this.subschedule_ID.size(otherOffset + size_0_otherOffset)
        this.subschedule_ID.sizeLemmaNextDWord(offset + size_0_offset, otherOffset + size_0_otherOffset)
        ()
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }
}


@inline @cCode.inline val ERR_TC_11_8_GROUPS_SUBSCHEDULE_ID: Int = 2111 /*(SIZE(0 .. maxTC-11-8-Group-ID-Count)) */
@inline @cCode.inline val ERR_TC_11_8_GROUPS_SUBSCHEDULE_ID_ELM_2: Int = 2106 /* */

@inline @cCode.inline val ERR_TC_11_8_GROUPS: Int = 2116 /* */

 
 

@inline @cCode.inline val ERR_ACN_ENCODE_TC_11_8_GROUPS: Int = 2119 /*  */
@inline @cCode.inline val ERR_ACN_ENCODE_TC_11_8_GROUPS_N2_UNINITIALIZED: Int = 2120 /*  */
@inline @cCode.inline val ERR_ACN_ENCODE_TC_11_8_GROUPS_N2: Int = 2099 /*  */
@inline @cCode.inline val ERR_ACN_ENCODE_TC_11_8_GROUPS_SUBSCHEDULE_ID: Int = 2114 /*  */
@inline @cCode.inline val ERR_ACN_ENCODE_TC_11_8_GROUPS_SUBSCHEDULE_ID_ELM_2: Int = 2109 /*  */
@inline @cCode.inline val TTC_11_8_Groups_REQUIRED_BYTES_FOR_ACN_ENCODING = 16384
@inline @cCode.inline val TTC_11_8_Groups_REQUIRED_BITS_FOR_ACN_ENCODING = 131072

@inline @cCode.inline val ERR_ACN_DECODE_TC_11_8_GROUPS: Int = 2121 /*  */
@inline @cCode.inline val ERR_ACN_DECODE_TC_11_8_GROUPS_N2: Int = 2100 /*  */
@inline @cCode.inline val ERR_ACN_DECODE_TC_11_8_GROUPS_SUBSCHEDULE_ID: Int = 2115 /*  */
@inline @cCode.inline val ERR_ACN_DECODE_TC_11_8_GROUPS_SUBSCHEDULE_ID_ELM_2: Int = 2110 /*  */
/*-- TTC_11_8_SubSchedules --------------------------------------------*/


case class TTC_11_8_SubSchedules_subschedule_ID (nCount: Int, arr: Vector[TSubSchedule_ID])
{
    require(this.arr.size <= 4095 && 0 <= this.nCount && this.nCount <= this.arr.size)

    def size(offset: Long): Long = {
        require((0L <= offset) && (offset <= 9223372036854644767L))
        TTC_11_8_SubSchedules_subschedule_ID.sizeRange(this.arr, offset, 0, this.nCount)
    }.ensuring { (res: Long) => 
        (0L <= res) && (res <= 131040L)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaAnyOffset(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854644767L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854644767L))
        TTC_11_8_SubSchedules_subschedule_ID.sizeLemmaAnyOffset(this.arr, offset, otherOffset, 0, this.nCount)
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextByte(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854644767L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854644767L))
        require((offset % 8L) == (otherOffset % 8L))
        TTC_11_8_SubSchedules_subschedule_ID.sizeLemmaNextByte(this.arr, offset, otherOffset, 0, this.nCount)
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextWord(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854644767L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854644767L))
        require((offset % 16L) == (otherOffset % 16L))
        TTC_11_8_SubSchedules_subschedule_ID.sizeLemmaNextWord(this.arr, offset, otherOffset, 0, this.nCount)
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextDWord(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854644767L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854644767L))
        require((offset % 32L) == (otherOffset % 32L))
        TTC_11_8_SubSchedules_subschedule_ID.sizeLemmaNextDWord(this.arr, offset, otherOffset, 0, this.nCount)
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }
}
object TTC_11_8_SubSchedules_subschedule_ID {
    def sizeRange(ls: Vector[ULong], offset: Long, from: Int, to: Int): Long = {
        require((0 <= from) && (from <= to) && (to <= ls.size) && (ls.size <= 4095))
        require((0L <= offset) && (offset <= 9223372036854775807L - 32L * (to - from)))
        decreases(to - from)
        if (from == to) {
            0L
        } else {
            val elemSize = 32L
            assert(elemSize == 32L)
            elemSize + TTC_11_8_SubSchedules_subschedule_ID.sizeRange(ls, offset + elemSize, from + 1, to)
        }
    }.ensuring { (res: Long) => 
        val nbElems = (to - from)
        (0L <= res && res <= 32L * nbElems)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaAnyOffset(ls: Vector[ULong], offset: Long, otherOffset: Long, from: Int, to: Int): Unit = {
        require((0 <= from) && (from <= to) && (to <= ls.size) && (ls.size <= 4095))
        require((0L <= offset) && (offset <= 9223372036854775807L - 32L * (to - from)))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854775807L - 32L * (to - from)))
        decreases(to - from)
        if (from == to) {
            ()
        } else {
            val elemSizeOff = 32L
            val elemSizeOtherOff = 32L
            assert(elemSizeOff == 32L)
            assert(elemSizeOtherOff == 32L)
            TTC_11_8_SubSchedules_subschedule_ID.sizeLemmaAnyOffset(ls, offset + elemSizeOff, otherOffset + elemSizeOtherOff, from + 1, to)
        }
    }.ensuring { (_: Unit) => 
        TTC_11_8_SubSchedules_subschedule_ID.sizeRange(ls, offset, from, to) == TTC_11_8_SubSchedules_subschedule_ID.sizeRange(ls, otherOffset, from, to)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextByte(ls: Vector[ULong], offset: Long, otherOffset: Long, from: Int, to: Int): Unit = {
        require((0 <= from) && (from <= to) && (to <= ls.size) && (ls.size <= 4095))
        require((0L <= offset) && (offset <= 9223372036854775807L - 32L * (to - from)))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854775807L - 32L * (to - from)))
        require((offset % 8L) == (otherOffset % 8L))
        decreases(to - from)
        if (from == to) {
            ()
        } else {
            val elemSizeOff = 32L
            val elemSizeOtherOff = 32L
            assert(elemSizeOff == 32L)
            assert(elemSizeOtherOff == 32L)
            TTC_11_8_SubSchedules_subschedule_ID.sizeLemmaNextByte(ls, offset + elemSizeOff, otherOffset + elemSizeOtherOff, from + 1, to)
        }
    }.ensuring { (_: Unit) => 
        TTC_11_8_SubSchedules_subschedule_ID.sizeRange(ls, offset, from, to) == TTC_11_8_SubSchedules_subschedule_ID.sizeRange(ls, otherOffset, from, to)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextWord(ls: Vector[ULong], offset: Long, otherOffset: Long, from: Int, to: Int): Unit = {
        require((0 <= from) && (from <= to) && (to <= ls.size) && (ls.size <= 4095))
        require((0L <= offset) && (offset <= 9223372036854775807L - 32L * (to - from)))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854775807L - 32L * (to - from)))
        require((offset % 16L) == (otherOffset % 16L))
        decreases(to - from)
        if (from == to) {
            ()
        } else {
            val elemSizeOff = 32L
            val elemSizeOtherOff = 32L
            assert(elemSizeOff == 32L)
            assert(elemSizeOtherOff == 32L)
            TTC_11_8_SubSchedules_subschedule_ID.sizeLemmaNextWord(ls, offset + elemSizeOff, otherOffset + elemSizeOtherOff, from + 1, to)
        }
    }.ensuring { (_: Unit) => 
        TTC_11_8_SubSchedules_subschedule_ID.sizeRange(ls, offset, from, to) == TTC_11_8_SubSchedules_subschedule_ID.sizeRange(ls, otherOffset, from, to)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextDWord(ls: Vector[ULong], offset: Long, otherOffset: Long, from: Int, to: Int): Unit = {
        require((0 <= from) && (from <= to) && (to <= ls.size) && (ls.size <= 4095))
        require((0L <= offset) && (offset <= 9223372036854775807L - 32L * (to - from)))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854775807L - 32L * (to - from)))
        require((offset % 32L) == (otherOffset % 32L))
        decreases(to - from)
        if (from == to) {
            ()
        } else {
            val elemSizeOff = 32L
            val elemSizeOtherOff = 32L
            assert(elemSizeOff == 32L)
            assert(elemSizeOtherOff == 32L)
            TTC_11_8_SubSchedules_subschedule_ID.sizeLemmaNextDWord(ls, offset + elemSizeOff, otherOffset + elemSizeOtherOff, from + 1, to)
        }
    }.ensuring { (_: Unit) => 
        TTC_11_8_SubSchedules_subschedule_ID.sizeRange(ls, offset, from, to) == TTC_11_8_SubSchedules_subschedule_ID.sizeRange(ls, otherOffset, from, to)
    }
}

case class TTC_11_8_SubSchedules (
    subschedule_ID: TTC_11_8_SubSchedules_subschedule_ID
) {

    def size(offset: Long): Long = {
        require((0L <= offset) && (offset <= 9223372036854644735L))
        val size_0 = 32L
        val size_1 = this.subschedule_ID.size(offset + size_0)
        size_0 + size_1
    }.ensuring { (res: Long) => 
        (0L <= res) && (res <= 131072L)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaAnyOffset(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854644735L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854644735L))
        val size_0_offset = 32L
        val size_0_otherOffset = 32L
        val size_1_offset = this.subschedule_ID.size(offset + size_0_offset)
        val size_1_otherOffset = this.subschedule_ID.size(otherOffset + size_0_otherOffset)
        this.subschedule_ID.sizeLemmaAnyOffset(offset + size_0_offset, otherOffset + size_0_otherOffset)
        ()
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextByte(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854644735L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854644735L))
        require((offset % 8L) == (otherOffset % 8L))
        val size_0_offset = 32L
        val size_0_otherOffset = 32L
        val size_1_offset = this.subschedule_ID.size(offset + size_0_offset)
        val size_1_otherOffset = this.subschedule_ID.size(otherOffset + size_0_otherOffset)
        this.subschedule_ID.sizeLemmaNextByte(offset + size_0_offset, otherOffset + size_0_otherOffset)
        ()
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextWord(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854644735L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854644735L))
        require((offset % 16L) == (otherOffset % 16L))
        val size_0_offset = 32L
        val size_0_otherOffset = 32L
        val size_1_offset = this.subschedule_ID.size(offset + size_0_offset)
        val size_1_otherOffset = this.subschedule_ID.size(otherOffset + size_0_otherOffset)
        this.subschedule_ID.sizeLemmaNextWord(offset + size_0_offset, otherOffset + size_0_otherOffset)
        ()
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextDWord(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854644735L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854644735L))
        require((offset % 32L) == (otherOffset % 32L))
        val size_0_offset = 32L
        val size_0_otherOffset = 32L
        val size_1_offset = this.subschedule_ID.size(offset + size_0_offset)
        val size_1_otherOffset = this.subschedule_ID.size(otherOffset + size_0_otherOffset)
        this.subschedule_ID.sizeLemmaNextDWord(offset + size_0_offset, otherOffset + size_0_otherOffset)
        ()
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }
}


@inline @cCode.inline val ERR_TC_11_8_SUBSCHEDULES_SUBSCHEDULE_ID: Int = 2088 /*(SIZE(0 .. maxTC-11-8-SubSchedule-ID-Count)) */
@inline @cCode.inline val ERR_TC_11_8_SUBSCHEDULES_SUBSCHEDULE_ID_ELM_2: Int = 2083 /* */

@inline @cCode.inline val ERR_TC_11_8_SUBSCHEDULES: Int = 2093 /* */

 
 

@inline @cCode.inline val ERR_ACN_ENCODE_TC_11_8_SUBSCHEDULES: Int = 2096 /*  */
@inline @cCode.inline val ERR_ACN_ENCODE_TC_11_8_SUBSCHEDULES_N1_UNINITIALIZED: Int = 2097 /*  */
@inline @cCode.inline val ERR_ACN_ENCODE_TC_11_8_SUBSCHEDULES_N1: Int = 2076 /*  */
@inline @cCode.inline val ERR_ACN_ENCODE_TC_11_8_SUBSCHEDULES_SUBSCHEDULE_ID: Int = 2091 /*  */
@inline @cCode.inline val ERR_ACN_ENCODE_TC_11_8_SUBSCHEDULES_SUBSCHEDULE_ID_ELM_2: Int = 2086 /*  */
@inline @cCode.inline val TTC_11_8_SubSchedules_REQUIRED_BYTES_FOR_ACN_ENCODING = 16384
@inline @cCode.inline val TTC_11_8_SubSchedules_REQUIRED_BITS_FOR_ACN_ENCODING = 131072

@inline @cCode.inline val ERR_ACN_DECODE_TC_11_8_SUBSCHEDULES: Int = 2098 /*  */
@inline @cCode.inline val ERR_ACN_DECODE_TC_11_8_SUBSCHEDULES_N1: Int = 2077 /*  */
@inline @cCode.inline val ERR_ACN_DECODE_TC_11_8_SUBSCHEDULES_SUBSCHEDULE_ID: Int = 2092 /*  */
@inline @cCode.inline val ERR_ACN_DECODE_TC_11_8_SUBSCHEDULES_SUBSCHEDULE_ID_ELM_2: Int = 2087 /*  */
/*-- TTC_11_8_TimeShiftTheScheduledActivitiesIdentifiedByAFilter --------------------------------------------*/

case class TTC_11_8_TimeShiftTheScheduledActivitiesIdentifiedByAFilter (
    timeOffset: TCDS_RelativeTime_PFC_2, 
    timeWindow: TTimeWindow, 
    subschedules: OptionMut[TTC_11_8_SubSchedules], 
    groups: OptionMut[TTC_11_8_Groups]
) {
    require(this.subschedules.isDefined && this.groups.isDefined)

    def size(offset: Long): Long = {
        require((0L <= offset) && (offset <= 9223372036854513469L))
        val size_0 = this.timeOffset.size(offset)
        val size_1 = this.timeWindow.size(offset + size_0)
        val size_2 = this.subschedules match {
            case SomeMut(v) =>
                v.size(offset + size_0 + size_1)
            case NoneMut() =>
                0L
        }
        val size_3 = this.groups match {
            case SomeMut(v) =>
                v.size(offset + size_0 + size_1 + size_2)
            case NoneMut() =>
                0L
        }
        size_0 + size_1 + size_2 + size_3
    }.ensuring { (res: Long) => 
        (0L <= res) && (res <= 262338L)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaAnyOffset(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854513469L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854513469L))
        val size_0_offset = this.timeOffset.size(offset)
        val size_0_otherOffset = this.timeOffset.size(otherOffset)
        this.timeOffset.sizeLemmaAnyOffset(offset, otherOffset)
        val size_1_offset = this.timeWindow.size(offset + size_0_offset)
        val size_1_otherOffset = this.timeWindow.size(otherOffset + size_0_otherOffset)
        this.timeWindow.sizeLemmaAnyOffset(offset + size_0_offset, otherOffset + size_0_otherOffset)
        val size_2_offset = this.subschedules match {
            case SomeMut(v) =>
                v.size(offset + size_0_offset + size_1_offset)
            case NoneMut() =>
                0L
        }
        val size_2_otherOffset = this.subschedules match {
            case SomeMut(v) =>
                v.size(otherOffset + size_0_otherOffset + size_1_otherOffset)
            case NoneMut() =>
                0L
        }
        this.subschedules match {
            case SomeMut(v) =>
                v.sizeLemmaAnyOffset(offset + size_0_offset + size_1_offset, otherOffset + size_0_otherOffset + size_1_otherOffset)
            case NoneMut() =>
                ()
        }
        val size_3_offset = this.groups match {
            case SomeMut(v) =>
                v.size(offset + size_0_offset + size_1_offset + size_2_offset)
            case NoneMut() =>
                0L
        }
        val size_3_otherOffset = this.groups match {
            case SomeMut(v) =>
                v.size(otherOffset + size_0_otherOffset + size_1_otherOffset + size_2_otherOffset)
            case NoneMut() =>
                0L
        }
        this.groups match {
            case SomeMut(v) =>
                v.sizeLemmaAnyOffset(offset + size_0_offset + size_1_offset + size_2_offset, otherOffset + size_0_otherOffset + size_1_otherOffset + size_2_otherOffset)
            case NoneMut() =>
                ()
        }
        ()
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextByte(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854513469L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854513469L))
        require((offset % 8L) == (otherOffset % 8L))
        val size_0_offset = this.timeOffset.size(offset)
        val size_0_otherOffset = this.timeOffset.size(otherOffset)
        this.timeOffset.sizeLemmaNextByte(offset, otherOffset)
        val size_1_offset = this.timeWindow.size(offset + size_0_offset)
        val size_1_otherOffset = this.timeWindow.size(otherOffset + size_0_otherOffset)
        this.timeWindow.sizeLemmaNextByte(offset + size_0_offset, otherOffset + size_0_otherOffset)
        val size_2_offset = this.subschedules match {
            case SomeMut(v) =>
                v.size(offset + size_0_offset + size_1_offset)
            case NoneMut() =>
                0L
        }
        val size_2_otherOffset = this.subschedules match {
            case SomeMut(v) =>
                v.size(otherOffset + size_0_otherOffset + size_1_otherOffset)
            case NoneMut() =>
                0L
        }
        this.subschedules match {
            case SomeMut(v) =>
                v.sizeLemmaNextByte(offset + size_0_offset + size_1_offset, otherOffset + size_0_otherOffset + size_1_otherOffset)
            case NoneMut() =>
                ()
        }
        val size_3_offset = this.groups match {
            case SomeMut(v) =>
                v.size(offset + size_0_offset + size_1_offset + size_2_offset)
            case NoneMut() =>
                0L
        }
        val size_3_otherOffset = this.groups match {
            case SomeMut(v) =>
                v.size(otherOffset + size_0_otherOffset + size_1_otherOffset + size_2_otherOffset)
            case NoneMut() =>
                0L
        }
        this.groups match {
            case SomeMut(v) =>
                v.sizeLemmaNextByte(offset + size_0_offset + size_1_offset + size_2_offset, otherOffset + size_0_otherOffset + size_1_otherOffset + size_2_otherOffset)
            case NoneMut() =>
                ()
        }
        ()
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextWord(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854513469L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854513469L))
        require((offset % 16L) == (otherOffset % 16L))
        val size_0_offset = this.timeOffset.size(offset)
        val size_0_otherOffset = this.timeOffset.size(otherOffset)
        this.timeOffset.sizeLemmaNextWord(offset, otherOffset)
        val size_1_offset = this.timeWindow.size(offset + size_0_offset)
        val size_1_otherOffset = this.timeWindow.size(otherOffset + size_0_otherOffset)
        this.timeWindow.sizeLemmaNextWord(offset + size_0_offset, otherOffset + size_0_otherOffset)
        val size_2_offset = this.subschedules match {
            case SomeMut(v) =>
                v.size(offset + size_0_offset + size_1_offset)
            case NoneMut() =>
                0L
        }
        val size_2_otherOffset = this.subschedules match {
            case SomeMut(v) =>
                v.size(otherOffset + size_0_otherOffset + size_1_otherOffset)
            case NoneMut() =>
                0L
        }
        this.subschedules match {
            case SomeMut(v) =>
                v.sizeLemmaNextWord(offset + size_0_offset + size_1_offset, otherOffset + size_0_otherOffset + size_1_otherOffset)
            case NoneMut() =>
                ()
        }
        val size_3_offset = this.groups match {
            case SomeMut(v) =>
                v.size(offset + size_0_offset + size_1_offset + size_2_offset)
            case NoneMut() =>
                0L
        }
        val size_3_otherOffset = this.groups match {
            case SomeMut(v) =>
                v.size(otherOffset + size_0_otherOffset + size_1_otherOffset + size_2_otherOffset)
            case NoneMut() =>
                0L
        }
        this.groups match {
            case SomeMut(v) =>
                v.sizeLemmaNextWord(offset + size_0_offset + size_1_offset + size_2_offset, otherOffset + size_0_otherOffset + size_1_otherOffset + size_2_otherOffset)
            case NoneMut() =>
                ()
        }
        ()
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextDWord(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854513469L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854513469L))
        require((offset % 32L) == (otherOffset % 32L))
        val size_0_offset = this.timeOffset.size(offset)
        val size_0_otherOffset = this.timeOffset.size(otherOffset)
        this.timeOffset.sizeLemmaNextDWord(offset, otherOffset)
        val size_1_offset = this.timeWindow.size(offset + size_0_offset)
        val size_1_otherOffset = this.timeWindow.size(otherOffset + size_0_otherOffset)
        this.timeWindow.sizeLemmaNextDWord(offset + size_0_offset, otherOffset + size_0_otherOffset)
        val size_2_offset = this.subschedules match {
            case SomeMut(v) =>
                v.size(offset + size_0_offset + size_1_offset)
            case NoneMut() =>
                0L
        }
        val size_2_otherOffset = this.subschedules match {
            case SomeMut(v) =>
                v.size(otherOffset + size_0_otherOffset + size_1_otherOffset)
            case NoneMut() =>
                0L
        }
        this.subschedules match {
            case SomeMut(v) =>
                v.sizeLemmaNextDWord(offset + size_0_offset + size_1_offset, otherOffset + size_0_otherOffset + size_1_otherOffset)
            case NoneMut() =>
                ()
        }
        val size_3_offset = this.groups match {
            case SomeMut(v) =>
                v.size(offset + size_0_offset + size_1_offset + size_2_offset)
            case NoneMut() =>
                0L
        }
        val size_3_otherOffset = this.groups match {
            case SomeMut(v) =>
                v.size(otherOffset + size_0_otherOffset + size_1_otherOffset + size_2_otherOffset)
            case NoneMut() =>
                0L
        }
        this.groups match {
            case SomeMut(v) =>
                v.sizeLemmaNextDWord(offset + size_0_offset + size_1_offset + size_2_offset, otherOffset + size_0_otherOffset + size_1_otherOffset + size_2_otherOffset)
            case NoneMut() =>
                ()
        }
        ()
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }
}


@inline @cCode.inline val ERR_TC_11_8_TIMESHIFTTHESCHEDULEDACTIVITIESIDENTIFIEDBYAFILTER: Int = 2071 /*(WITH COMPONENTS
{
    subschedules PRESENT,
    groups PRESENT
}) */
@inline @cCode.inline val ERR_TC_11_8_TIMESHIFTTHESCHEDULEDACTIVITIESIDENTIFIEDBYAFILTER_TIMEOFFSET_2_2: Int = 1863 /* */
@inline @cCode.inline val ERR_TC_11_8_TIMESHIFTTHESCHEDULEDACTIVITIESIDENTIFIEDBYAFILTER_TIMEWINDOW_2: Int = 2012 /* */
@inline @cCode.inline val ERR_TC_11_8_TIMESHIFTTHESCHEDULEDACTIVITIESIDENTIFIEDBYAFILTER_SUBSCHEDULES_2: Int = 2039 /* */
@inline @cCode.inline val ERR_TC_11_8_TIMESHIFTTHESCHEDULEDACTIVITIESIDENTIFIEDBYAFILTER_GROUPS_2: Int = 2066 /* */

 

@inline @cCode.inline val ERR_ACN_ENCODE_TC_11_8_TIMESHIFTTHESCHEDULEDACTIVITIESIDENTIFIEDBYAFILTER: Int = 2074 /*  */
@inline @cCode.inline val ERR_ACN_ENCODE_TC_11_8_TIMESHIFTTHESCHEDULEDACTIVITIESIDENTIFIEDBYAFILTER_TIMEOFFSET_2_2: Int = 1866 /*  */
@inline @cCode.inline val ERR_ACN_ENCODE_TC_11_8_TIMESHIFTTHESCHEDULEDACTIVITIESIDENTIFIEDBYAFILTER_TIMEWINDOW_2: Int = 2015 /*  */
@inline @cCode.inline val ERR_ACN_ENCODE_TC_11_8_TIMESHIFTTHESCHEDULEDACTIVITIESIDENTIFIEDBYAFILTER_SUBSCHEDULES_2: Int = 2042 /*  */
@inline @cCode.inline val ERR_ACN_ENCODE_TC_11_8_TIMESHIFTTHESCHEDULEDACTIVITIESIDENTIFIEDBYAFILTER_GROUPS_2: Int = 2069 /*  */
@inline @cCode.inline val TTC_11_8_TimeShiftTheScheduledActivitiesIdentifiedByAFilter_REQUIRED_BYTES_FOR_ACN_ENCODING = 32793
@inline @cCode.inline val TTC_11_8_TimeShiftTheScheduledActivitiesIdentifiedByAFilter_REQUIRED_BITS_FOR_ACN_ENCODING = 262338

@inline @cCode.inline val ERR_ACN_DECODE_TC_11_8_TIMESHIFTTHESCHEDULEDACTIVITIESIDENTIFIEDBYAFILTER: Int = 2075 /*  */
@inline @cCode.inline val ERR_ACN_DECODE_TC_11_8_TIMESHIFTTHESCHEDULEDACTIVITIESIDENTIFIEDBYAFILTER_TIMEOFFSET_2_2: Int = 1867 /*  */
@inline @cCode.inline val ERR_ACN_DECODE_TC_11_8_TIMESHIFTTHESCHEDULEDACTIVITIESIDENTIFIEDBYAFILTER_TIMEWINDOW_2: Int = 2016 /*  */
@inline @cCode.inline val ERR_ACN_DECODE_TC_11_8_TIMESHIFTTHESCHEDULEDACTIVITIESIDENTIFIEDBYAFILTER_SUBSCHEDULES_2: Int = 2043 /*  */
@inline @cCode.inline val ERR_ACN_DECODE_TC_11_8_TIMESHIFTTHESCHEDULEDACTIVITIESIDENTIFIEDBYAFILTER_GROUPS_2: Int = 2070 /*  */

// @inline @cCode.inline val maxTC_11_8_Group_ID_Count: Int = 4095 // defined in body
// @inline @cCode.inline val maxTC_11_8_SubSchedule_ID_Count: Int = 4095 // defined in body

