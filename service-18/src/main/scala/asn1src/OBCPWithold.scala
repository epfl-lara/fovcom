/*
Code automatically generated by asn1scc tool
*/
package asn1src

import asn1scala._
import stainless.lang.{ghost => ghostExpr, _}
import stainless.annotation._
import stainless.collection._
import stainless.proof._
import StaticChecks._


def TOBCPWithold_IsConstraintValid(pVal: TOBCPWithold): Either[ErrorCode, Int] =
{
    var ret: Either[ErrorCode, Int] = Right(0)
    ret = TOBCP_ID_IsConstraintValid(pVal.obcp_ID)
    if ret.isRight then
        ret = TOBCPStep_ID_IsConstraintValid(pVal.step_ID)
    ret
}

def TOBCPWithold_Initialize(): TOBCPWithold = TOBCPWithold(obcp_ID = Vector.fill[UByte](5.toInt+1)(0x0.toRawUByte), step_ID = TOBCPStep_ID.TstopAtEndOfCurrentStep)

@opaque @inlineOnce 
def TOBCPWithold_ACN_Encode(pVal: TOBCPWithold, codec: ACN, bCheckConstraints: Boolean): Either[ErrorCode, Int] =  // acn:21
{
    require(codec.base.bitStream.validate_offset_bits(37L))
    TOBCPWithold_IsConstraintValid(pVal) match
        case Left(l) => return Left(l)
        case Right(_) =>
    @ghost val oldCdc = snapshot(codec)
    @ghost val codec_0_1 = snapshot(codec)
    /* Encode obcp_ID */
    TOBCP_ID_ACN_Encode(pVal.obcp_ID, codec, false) match // uper:6
        case Right(_) =>
        case Left(err) => return Left(err)
    ghostExpr {
        BitStream.validateOffsetBitsIneqLemma(codec_0_1.base.bitStream, codec.base.bitStream, 37L, 35L)
        check(codec.base.bitStream.bitIndex <= oldCdc.base.bitStream.bitIndex + 35L)
        check(codec.base.buf.length == oldCdc.base.buf.length)
    }
    @ghost val size_0 = 35L
    ghostExpr {
        check(codec.base.bitStream.bitIndex == oldCdc.base.bitStream.bitIndex + size_0)
    }
    @ghost val codec_0_2 = snapshot(codec)
    /* Encode step_ID */
    TOBCPStep_ID_ACN_Encode(pVal.step_ID, codec, false) match // uper:6
        case Right(_) =>
        case Left(err) => return Left(err)
    ghostExpr {
        check(codec.base.bitStream.bitIndex <= oldCdc.base.bitStream.bitIndex + 37L)
        check(codec.base.buf.length == oldCdc.base.buf.length)
    }
    @ghost val size_1 = 2L
    ghostExpr {
        check(codec.base.bitStream.bitIndex == oldCdc.base.bitStream.bitIndex + size_0 + size_1)
    }
    ghostExpr {
        check(codec.base.bitStream.bitIndex <= codec_0_1.base.bitStream.bitIndex + 37L)
    }
    ghostExpr {
        check(pVal.size(oldCdc.base.bitStream.bitIndex) == size_0 + size_1)
    }
    Right(0)
}.ensuring { (res : Either[ErrorCode, Int]) =>
    res match {
        case Left(_) =>
            true
        case Right(_) =>
            old(codec).base.buf.length == codec.base.buf.length && codec.base.bitStream.bitIndex == old(codec).base.bitStream.bitIndex + pVal.size(old(codec).base.bitStream.bitIndex)
    }
}

@opaque @inlineOnce 
def TOBCPWithold_ACN_Decode(codec: ACN): EitherMut[ErrorCode, TOBCPWithold] =
{
    require(codec.base.bitStream.validate_offset_bits(37L))

    @ghost val oldCdc = snapshot(codec)
    @ghost val codec_0_1 = snapshot(codec)
    /* Decode obcp_ID */
    val pVal_obcp_ID = TOBCP_ID_ACN_Decode(codec) match // uper:13
        case RightMut(decData) => decData
        case LeftMut(err) => return LeftMut(err)
    ghostExpr {
        BitStream.validateOffsetBitsIneqLemma(codec_0_1.base.bitStream, codec.base.bitStream, 37L, 35L)
        check(codec.base.bitStream.bitIndex <= oldCdc.base.bitStream.bitIndex + 35L)
        check(codec.base.buf == oldCdc.base.buf)
    }
    @ghost val size_0 = 35L
    ghostExpr {
        check(codec.base.bitStream.bitIndex == oldCdc.base.bitStream.bitIndex + size_0)
    }
    @ghost val codec_0_2 = snapshot(codec)
    /* Decode step_ID */
    val pVal_step_ID = TOBCPStep_ID_ACN_Decode(codec) match // uper:13
        case RightMut(decData) => decData
        case LeftMut(err) => return LeftMut(err)
    ghostExpr {
        check(codec.base.bitStream.bitIndex <= oldCdc.base.bitStream.bitIndex + 37L)
        check(codec.base.buf == oldCdc.base.buf)
    }
    @ghost val size_1 = 2L
    ghostExpr {
        check(codec.base.bitStream.bitIndex == oldCdc.base.bitStream.bitIndex + size_0 + size_1)
    }
    ghostExpr {
        check(codec.base.bitStream.bitIndex <= codec_0_1.base.bitStream.bitIndex + 37L)
    }
    val pVal = TOBCPWithold(pVal_obcp_ID, pVal_step_ID)
    ghostExpr {
        check(pVal.size(oldCdc.base.bitStream.bitIndex) == size_0 + size_1)
    }

    TOBCPWithold_IsConstraintValid(pVal) match
        case Left(l) => LeftMut[ErrorCode, TOBCPWithold](l)
        case Right(_) => RightMut[ErrorCode, TOBCPWithold](pVal)
}.ensuring { (res : EitherMut[ErrorCode, TOBCPWithold]) =>
    res match {
        case LeftMut(_) =>
            true
        case RightMut(resVal) =>
            old(codec).base.buf == codec.base.buf && codec.base.bitStream.bitIndex == old(codec).base.bitStream.bitIndex + resVal.size(old(codec).base.bitStream.bitIndex) && TOBCPWithold_IsConstraintValid(resVal).isRight
    }
}

@ghost @pure 
def TOBCPWithold_ACN_Decode_pure(codec: ACN): (ACN, EitherMut[ErrorCode, TOBCPWithold]) =
{
    require(codec.base.bitStream.validate_offset_bits(37L))
    val cpy = snapshot(codec)
    val res = TOBCPWithold_ACN_Decode(cpy)
    (cpy, res)
}