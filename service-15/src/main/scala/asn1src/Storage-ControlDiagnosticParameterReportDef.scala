/*
Code automatically generated by asn1scc tool
*/
package asn1src

import asn1scala._
import stainless.lang._
import stainless.annotation._
import stainless.collection._
import stainless.proof._
import StaticChecks._

/*-- TStorage_ControlDiagnosticParameterReportItem --------------------------------------------*/

case class TStorage_ControlDiagnosticParameterReportItem (
    parameter: TDiagnosticParameterReportStructure_ID, 
    subsamplingRate: OptionMut[TPUSC_UINT32]
) {

    def size(offset: Long): Long = {
        require((0L <= offset) && (offset <= 9223372036854775742L))
        val size_0 = 1L
        val size_1 = 32L
        val size_2 = this.subsamplingRate match {
            case SomeMut(v) =>
                32L
            case NoneMut() =>
                0L
        }
        size_0 + size_1 + size_2
    }.ensuring { (res: Long) => 
        (0L <= res) && (res <= 65L)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaAnyOffset(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854775742L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854775742L))
        val size_0_offset = 1L
        val size_0_otherOffset = 1L
        val size_1_offset = 32L
        val size_1_otherOffset = 32L
        val size_2_offset = this.subsamplingRate match {
            case SomeMut(v) =>
                32L
            case NoneMut() =>
                0L
        }
        val size_2_otherOffset = this.subsamplingRate match {
            case SomeMut(v) =>
                32L
            case NoneMut() =>
                0L
        }
        ()
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextByte(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854775742L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854775742L))
        require((offset % 8L) == (otherOffset % 8L))
        val size_0_offset = 1L
        val size_0_otherOffset = 1L
        val size_1_offset = 32L
        val size_1_otherOffset = 32L
        val size_2_offset = this.subsamplingRate match {
            case SomeMut(v) =>
                32L
            case NoneMut() =>
                0L
        }
        val size_2_otherOffset = this.subsamplingRate match {
            case SomeMut(v) =>
                32L
            case NoneMut() =>
                0L
        }
        ()
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextWord(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854775742L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854775742L))
        require((offset % 16L) == (otherOffset % 16L))
        val size_0_offset = 1L
        val size_0_otherOffset = 1L
        val size_1_offset = 32L
        val size_1_otherOffset = 32L
        val size_2_offset = this.subsamplingRate match {
            case SomeMut(v) =>
                32L
            case NoneMut() =>
                0L
        }
        val size_2_otherOffset = this.subsamplingRate match {
            case SomeMut(v) =>
                32L
            case NoneMut() =>
                0L
        }
        ()
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextDWord(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854775742L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854775742L))
        require((offset % 32L) == (otherOffset % 32L))
        val size_0_offset = 1L
        val size_0_otherOffset = 1L
        val size_1_offset = 32L
        val size_1_otherOffset = 32L
        val size_2_offset = this.subsamplingRate match {
            case SomeMut(v) =>
                32L
            case NoneMut() =>
                0L
        }
        val size_2_otherOffset = this.subsamplingRate match {
            case SomeMut(v) =>
                32L
            case NoneMut() =>
                0L
        }
        ()
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }
}


@inline @cCode.inline val ERR_STORAGE_CONTROLDIAGNOSTICPARAMETERREPORTITEM: Int = 2779 /* */
@inline @cCode.inline val ERR_STORAGE_CONTROLDIAGNOSTICPARAMETERREPORTITEM_PARAMETER_2: Int = 2764 /* */
@inline @cCode.inline val ERR_STORAGE_CONTROLDIAGNOSTICPARAMETERREPORTITEM_SUBSAMPLINGRATE_2: Int = 2774 /* */

 

@inline @cCode.inline val ERR_ACN_ENCODE_STORAGE_CONTROLDIAGNOSTICPARAMETERREPORTITEM: Int = 2782 /*  */
@inline @cCode.inline val ERR_ACN_ENCODE_STORAGE_CONTROLDIAGNOSTICPARAMETERREPORTITEM_PARAMETER_2: Int = 2767 /*  */
@inline @cCode.inline val ERR_ACN_ENCODE_STORAGE_CONTROLDIAGNOSTICPARAMETERREPORTITEM_SUBSAMPLINGRATE_2: Int = 2777 /*  */
@inline @cCode.inline val TStorage_ControlDiagnosticParameterReportItem_REQUIRED_BYTES_FOR_ACN_ENCODING = 9
@inline @cCode.inline val TStorage_ControlDiagnosticParameterReportItem_REQUIRED_BITS_FOR_ACN_ENCODING = 65

@inline @cCode.inline val ERR_ACN_DECODE_STORAGE_CONTROLDIAGNOSTICPARAMETERREPORTITEM: Int = 2783 /*  */
@inline @cCode.inline val ERR_ACN_DECODE_STORAGE_CONTROLDIAGNOSTICPARAMETERREPORTITEM_PARAMETER_2: Int = 2768 /*  */
@inline @cCode.inline val ERR_ACN_DECODE_STORAGE_CONTROLDIAGNOSTICPARAMETERREPORTITEM_SUBSAMPLINGRATE_2: Int = 2778 /*  */
/*-- TStorage_ControlDiagnosticParameterReportConfiguration --------------------------------------------*/


case class TStorage_ControlDiagnosticParameterReportConfiguration_tail (nCount: Int, arr: Vector[TStorage_ControlDiagnosticParameterReportItem])
{
    require(this.arr.size <= 31 && 0 <= this.nCount && this.nCount <= this.arr.size)

    def size(offset: Long): Long = {
        require((0L <= offset) && (offset <= 9223372036854773792L))
        TStorage_ControlDiagnosticParameterReportConfiguration_tail.sizeRange(this.arr, offset, 0, this.nCount)
    }.ensuring { (res: Long) => 
        (0L <= res) && (res <= 2015L)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaAnyOffset(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854773792L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854773792L))
        TStorage_ControlDiagnosticParameterReportConfiguration_tail.sizeLemmaAnyOffset(this.arr, offset, otherOffset, 0, this.nCount)
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextByte(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854773792L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854773792L))
        require((offset % 8L) == (otherOffset % 8L))
        TStorage_ControlDiagnosticParameterReportConfiguration_tail.sizeLemmaNextByte(this.arr, offset, otherOffset, 0, this.nCount)
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextWord(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854773792L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854773792L))
        require((offset % 16L) == (otherOffset % 16L))
        TStorage_ControlDiagnosticParameterReportConfiguration_tail.sizeLemmaNextWord(this.arr, offset, otherOffset, 0, this.nCount)
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextDWord(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854773792L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854773792L))
        require((offset % 32L) == (otherOffset % 32L))
        TStorage_ControlDiagnosticParameterReportConfiguration_tail.sizeLemmaNextDWord(this.arr, offset, otherOffset, 0, this.nCount)
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }
}
object TStorage_ControlDiagnosticParameterReportConfiguration_tail {
    def sizeRange(ls: Vector[TStorage_ControlDiagnosticParameterReportItem], offset: Long, from: Int, to: Int): Long = {
        require((0 <= from) && (from <= to) && (to <= ls.size) && (ls.size <= 31))
        require((0L <= offset) && (offset <= 9223372036854775807L - 65L * (to - from)))
        decreases(to - from)
        if (from == to) {
            0L
        } else {
            val elemSize = ls.apply(from).size(offset)
            assert(0L <= elemSize && elemSize <= 65L)
            elemSize + TStorage_ControlDiagnosticParameterReportConfiguration_tail.sizeRange(ls, offset + elemSize, from + 1, to)
        }
    }.ensuring { (res: Long) => 
        val nbElems = (to - from)
        (0L <= res && res <= 65L * nbElems)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaAnyOffset(ls: Vector[TStorage_ControlDiagnosticParameterReportItem], offset: Long, otherOffset: Long, from: Int, to: Int): Unit = {
        require((0 <= from) && (from <= to) && (to <= ls.size) && (ls.size <= 31))
        require((0L <= offset) && (offset <= 9223372036854775807L - 65L * (to - from)))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854775807L - 65L * (to - from)))
        decreases(to - from)
        if (from == to) {
            ()
        } else {
            val elemSizeOff = ls.apply(from).size(offset)
            val elemSizeOtherOff = ls.apply(from).size(otherOffset)
            assert(0L <= elemSizeOff && elemSizeOff <= 65L)
            assert(0L <= elemSizeOtherOff && elemSizeOtherOff <= 65L)
            ls.apply(from).sizeLemmaAnyOffset(offset, otherOffset)
            TStorage_ControlDiagnosticParameterReportConfiguration_tail.sizeLemmaAnyOffset(ls, offset + elemSizeOff, otherOffset + elemSizeOtherOff, from + 1, to)
        }
    }.ensuring { (_: Unit) => 
        TStorage_ControlDiagnosticParameterReportConfiguration_tail.sizeRange(ls, offset, from, to) == TStorage_ControlDiagnosticParameterReportConfiguration_tail.sizeRange(ls, otherOffset, from, to)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextByte(ls: Vector[TStorage_ControlDiagnosticParameterReportItem], offset: Long, otherOffset: Long, from: Int, to: Int): Unit = {
        require((0 <= from) && (from <= to) && (to <= ls.size) && (ls.size <= 31))
        require((0L <= offset) && (offset <= 9223372036854775807L - 65L * (to - from)))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854775807L - 65L * (to - from)))
        require((offset % 8L) == (otherOffset % 8L))
        decreases(to - from)
        if (from == to) {
            ()
        } else {
            val elemSizeOff = ls.apply(from).size(offset)
            val elemSizeOtherOff = ls.apply(from).size(otherOffset)
            assert(0L <= elemSizeOff && elemSizeOff <= 65L)
            assert(0L <= elemSizeOtherOff && elemSizeOtherOff <= 65L)
            ls.apply(from).sizeLemmaNextByte(offset, otherOffset)
            TStorage_ControlDiagnosticParameterReportConfiguration_tail.sizeLemmaNextByte(ls, offset + elemSizeOff, otherOffset + elemSizeOtherOff, from + 1, to)
        }
    }.ensuring { (_: Unit) => 
        TStorage_ControlDiagnosticParameterReportConfiguration_tail.sizeRange(ls, offset, from, to) == TStorage_ControlDiagnosticParameterReportConfiguration_tail.sizeRange(ls, otherOffset, from, to)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextWord(ls: Vector[TStorage_ControlDiagnosticParameterReportItem], offset: Long, otherOffset: Long, from: Int, to: Int): Unit = {
        require((0 <= from) && (from <= to) && (to <= ls.size) && (ls.size <= 31))
        require((0L <= offset) && (offset <= 9223372036854775807L - 65L * (to - from)))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854775807L - 65L * (to - from)))
        require((offset % 16L) == (otherOffset % 16L))
        decreases(to - from)
        if (from == to) {
            ()
        } else {
            val elemSizeOff = ls.apply(from).size(offset)
            val elemSizeOtherOff = ls.apply(from).size(otherOffset)
            assert(0L <= elemSizeOff && elemSizeOff <= 65L)
            assert(0L <= elemSizeOtherOff && elemSizeOtherOff <= 65L)
            ls.apply(from).sizeLemmaNextWord(offset, otherOffset)
            TStorage_ControlDiagnosticParameterReportConfiguration_tail.sizeLemmaNextWord(ls, offset + elemSizeOff, otherOffset + elemSizeOtherOff, from + 1, to)
        }
    }.ensuring { (_: Unit) => 
        TStorage_ControlDiagnosticParameterReportConfiguration_tail.sizeRange(ls, offset, from, to) == TStorage_ControlDiagnosticParameterReportConfiguration_tail.sizeRange(ls, otherOffset, from, to)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextDWord(ls: Vector[TStorage_ControlDiagnosticParameterReportItem], offset: Long, otherOffset: Long, from: Int, to: Int): Unit = {
        require((0 <= from) && (from <= to) && (to <= ls.size) && (ls.size <= 31))
        require((0L <= offset) && (offset <= 9223372036854775807L - 65L * (to - from)))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854775807L - 65L * (to - from)))
        require((offset % 32L) == (otherOffset % 32L))
        decreases(to - from)
        if (from == to) {
            ()
        } else {
            val elemSizeOff = ls.apply(from).size(offset)
            val elemSizeOtherOff = ls.apply(from).size(otherOffset)
            assert(0L <= elemSizeOff && elemSizeOff <= 65L)
            assert(0L <= elemSizeOtherOff && elemSizeOtherOff <= 65L)
            ls.apply(from).sizeLemmaNextDWord(offset, otherOffset)
            TStorage_ControlDiagnosticParameterReportConfiguration_tail.sizeLemmaNextDWord(ls, offset + elemSizeOff, otherOffset + elemSizeOtherOff, from + 1, to)
        }
    }.ensuring { (_: Unit) => 
        TStorage_ControlDiagnosticParameterReportConfiguration_tail.sizeRange(ls, offset, from, to) == TStorage_ControlDiagnosticParameterReportConfiguration_tail.sizeRange(ls, otherOffset, from, to)
    }
}

case class TStorage_ControlDiagnosticParameterReportConfiguration (
    applicationProcess_ID: OptionMut[TApplicationProcess_ID], 
    tail: TStorage_ControlDiagnosticParameterReportConfiguration_tail
) {

    def size(offset: Long): Long = {
        require((0L <= offset) && (offset <= 9223372036854773748L))
        val size_0 = 1L
        val size_1 = this.applicationProcess_ID match {
            case SomeMut(v) =>
                11L
            case NoneMut() =>
                0L
        }
        val size_2 = 32L
        val size_3 = this.tail.size(offset + size_0 + size_1 + size_2)
        size_0 + size_1 + size_2 + size_3
    }.ensuring { (res: Long) => 
        (0L <= res) && (res <= 2059L)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaAnyOffset(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854773748L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854773748L))
        val size_0_offset = 1L
        val size_0_otherOffset = 1L
        val size_1_offset = this.applicationProcess_ID match {
            case SomeMut(v) =>
                11L
            case NoneMut() =>
                0L
        }
        val size_1_otherOffset = this.applicationProcess_ID match {
            case SomeMut(v) =>
                11L
            case NoneMut() =>
                0L
        }
        val size_2_offset = 32L
        val size_2_otherOffset = 32L
        val size_3_offset = this.tail.size(offset + size_0_offset + size_1_offset + size_2_offset)
        val size_3_otherOffset = this.tail.size(otherOffset + size_0_otherOffset + size_1_otherOffset + size_2_otherOffset)
        this.tail.sizeLemmaAnyOffset(offset + size_0_offset + size_1_offset + size_2_offset, otherOffset + size_0_otherOffset + size_1_otherOffset + size_2_otherOffset)
        ()
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextByte(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854773748L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854773748L))
        require((offset % 8L) == (otherOffset % 8L))
        val size_0_offset = 1L
        val size_0_otherOffset = 1L
        val size_1_offset = this.applicationProcess_ID match {
            case SomeMut(v) =>
                11L
            case NoneMut() =>
                0L
        }
        val size_1_otherOffset = this.applicationProcess_ID match {
            case SomeMut(v) =>
                11L
            case NoneMut() =>
                0L
        }
        val size_2_offset = 32L
        val size_2_otherOffset = 32L
        val size_3_offset = this.tail.size(offset + size_0_offset + size_1_offset + size_2_offset)
        val size_3_otherOffset = this.tail.size(otherOffset + size_0_otherOffset + size_1_otherOffset + size_2_otherOffset)
        this.tail.sizeLemmaNextByte(offset + size_0_offset + size_1_offset + size_2_offset, otherOffset + size_0_otherOffset + size_1_otherOffset + size_2_otherOffset)
        ()
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextWord(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854773748L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854773748L))
        require((offset % 16L) == (otherOffset % 16L))
        val size_0_offset = 1L
        val size_0_otherOffset = 1L
        val size_1_offset = this.applicationProcess_ID match {
            case SomeMut(v) =>
                11L
            case NoneMut() =>
                0L
        }
        val size_1_otherOffset = this.applicationProcess_ID match {
            case SomeMut(v) =>
                11L
            case NoneMut() =>
                0L
        }
        val size_2_offset = 32L
        val size_2_otherOffset = 32L
        val size_3_offset = this.tail.size(offset + size_0_offset + size_1_offset + size_2_offset)
        val size_3_otherOffset = this.tail.size(otherOffset + size_0_otherOffset + size_1_otherOffset + size_2_otherOffset)
        this.tail.sizeLemmaNextWord(offset + size_0_offset + size_1_offset + size_2_offset, otherOffset + size_0_otherOffset + size_1_otherOffset + size_2_otherOffset)
        ()
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextDWord(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854773748L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854773748L))
        require((offset % 32L) == (otherOffset % 32L))
        val size_0_offset = 1L
        val size_0_otherOffset = 1L
        val size_1_offset = this.applicationProcess_ID match {
            case SomeMut(v) =>
                11L
            case NoneMut() =>
                0L
        }
        val size_1_otherOffset = this.applicationProcess_ID match {
            case SomeMut(v) =>
                11L
            case NoneMut() =>
                0L
        }
        val size_2_offset = 32L
        val size_2_otherOffset = 32L
        val size_3_offset = this.tail.size(offset + size_0_offset + size_1_offset + size_2_offset)
        val size_3_otherOffset = this.tail.size(otherOffset + size_0_otherOffset + size_1_otherOffset + size_2_otherOffset)
        this.tail.sizeLemmaNextDWord(offset + size_0_offset + size_1_offset + size_2_offset, otherOffset + size_0_otherOffset + size_1_otherOffset + size_2_otherOffset)
        ()
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }
}


@inline @cCode.inline val ERR_STORAGE_CONTROLDIAGNOSTICPARAMETERREPORTCONFIGURATION_TAIL: Int = 2748 /*(SIZE(0 .. maxTailSize)) */
@inline @cCode.inline val ERR_STORAGE_CONTROLDIAGNOSTICPARAMETERREPORTCONFIGURATION_TAIL_ELM_2: Int = 2743 /* */

@inline @cCode.inline val ERR_STORAGE_CONTROLDIAGNOSTICPARAMETERREPORTCONFIGURATION: Int = 2753 /* */
@inline @cCode.inline val ERR_STORAGE_CONTROLDIAGNOSTICPARAMETERREPORTCONFIGURATION_APPLICATIONPROCESS_ID_2: Int = 2711 /* */

 
 

@inline @cCode.inline val ERR_ACN_ENCODE_STORAGE_CONTROLDIAGNOSTICPARAMETERREPORTCONFIGURATION: Int = 2756 /*  */
@inline @cCode.inline val ERR_ACN_ENCODE_STORAGE_CONTROLDIAGNOSTICPARAMETERREPORTCONFIGURATION_APPLICATIONPROCESS_ID_2: Int = 2714 /*  */
@inline @cCode.inline val ERR_ACN_ENCODE_STORAGE_CONTROLDIAGNOSTICPARAMETERREPORTCONFIGURATION_N2_UNINITIALIZED: Int = 2757 /*  */
@inline @cCode.inline val ERR_ACN_ENCODE_STORAGE_CONTROLDIAGNOSTICPARAMETERREPORTCONFIGURATION_N2: Int = 2716 /*  */
@inline @cCode.inline val ERR_ACN_ENCODE_STORAGE_CONTROLDIAGNOSTICPARAMETERREPORTCONFIGURATION_TAIL: Int = 2751 /*  */
@inline @cCode.inline val ERR_ACN_ENCODE_STORAGE_CONTROLDIAGNOSTICPARAMETERREPORTCONFIGURATION_TAIL_ELM_2: Int = 2746 /*  */
@inline @cCode.inline val TStorage_ControlDiagnosticParameterReportConfiguration_REQUIRED_BYTES_FOR_ACN_ENCODING = 258
@inline @cCode.inline val TStorage_ControlDiagnosticParameterReportConfiguration_REQUIRED_BITS_FOR_ACN_ENCODING = 2059

@inline @cCode.inline val ERR_ACN_DECODE_STORAGE_CONTROLDIAGNOSTICPARAMETERREPORTCONFIGURATION: Int = 2758 /*  */
@inline @cCode.inline val ERR_ACN_DECODE_STORAGE_CONTROLDIAGNOSTICPARAMETERREPORTCONFIGURATION_APPLICATIONPROCESS_ID_2: Int = 2715 /*  */
@inline @cCode.inline val ERR_ACN_DECODE_STORAGE_CONTROLDIAGNOSTICPARAMETERREPORTCONFIGURATION_N2: Int = 2717 /*  */
@inline @cCode.inline val ERR_ACN_DECODE_STORAGE_CONTROLDIAGNOSTICPARAMETERREPORTCONFIGURATION_TAIL: Int = 2752 /*  */
@inline @cCode.inline val ERR_ACN_DECODE_STORAGE_CONTROLDIAGNOSTICPARAMETERREPORTCONFIGURATION_TAIL_ELM_2: Int = 2747 /*  */
/*-- TStorage_ControlDiagnosticParameterReport --------------------------------------------*/


case class TStorage_ControlDiagnosticParameterReport_configuration (nCount: Int, arr: Vector[TStorage_ControlDiagnosticParameterReportConfiguration])
{
    require(this.arr.size <= 31 && 0 <= this.nCount && this.nCount <= this.arr.size)

    def size(offset: Long): Long = {
        require((0L <= offset) && (offset <= 9223372036854711978L))
        TStorage_ControlDiagnosticParameterReport_configuration.sizeRange(this.arr, offset, 0, this.nCount)
    }.ensuring { (res: Long) => 
        (0L <= res) && (res <= 63829L)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaAnyOffset(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854711978L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854711978L))
        TStorage_ControlDiagnosticParameterReport_configuration.sizeLemmaAnyOffset(this.arr, offset, otherOffset, 0, this.nCount)
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextByte(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854711978L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854711978L))
        require((offset % 8L) == (otherOffset % 8L))
        TStorage_ControlDiagnosticParameterReport_configuration.sizeLemmaNextByte(this.arr, offset, otherOffset, 0, this.nCount)
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextWord(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854711978L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854711978L))
        require((offset % 16L) == (otherOffset % 16L))
        TStorage_ControlDiagnosticParameterReport_configuration.sizeLemmaNextWord(this.arr, offset, otherOffset, 0, this.nCount)
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextDWord(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854711978L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854711978L))
        require((offset % 32L) == (otherOffset % 32L))
        TStorage_ControlDiagnosticParameterReport_configuration.sizeLemmaNextDWord(this.arr, offset, otherOffset, 0, this.nCount)
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }
}
object TStorage_ControlDiagnosticParameterReport_configuration {
    def sizeRange(ls: Vector[TStorage_ControlDiagnosticParameterReportConfiguration], offset: Long, from: Int, to: Int): Long = {
        require((0 <= from) && (from <= to) && (to <= ls.size) && (ls.size <= 31))
        require((0L <= offset) && (offset <= 9223372036854775807L - 2059L * (to - from)))
        decreases(to - from)
        if (from == to) {
            0L
        } else {
            val elemSize = ls.apply(from).size(offset)
            assert(0L <= elemSize && elemSize <= 2059L)
            elemSize + TStorage_ControlDiagnosticParameterReport_configuration.sizeRange(ls, offset + elemSize, from + 1, to)
        }
    }.ensuring { (res: Long) => 
        val nbElems = (to - from)
        (0L <= res && res <= 2059L * nbElems)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaAnyOffset(ls: Vector[TStorage_ControlDiagnosticParameterReportConfiguration], offset: Long, otherOffset: Long, from: Int, to: Int): Unit = {
        require((0 <= from) && (from <= to) && (to <= ls.size) && (ls.size <= 31))
        require((0L <= offset) && (offset <= 9223372036854775807L - 2059L * (to - from)))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854775807L - 2059L * (to - from)))
        decreases(to - from)
        if (from == to) {
            ()
        } else {
            val elemSizeOff = ls.apply(from).size(offset)
            val elemSizeOtherOff = ls.apply(from).size(otherOffset)
            assert(0L <= elemSizeOff && elemSizeOff <= 2059L)
            assert(0L <= elemSizeOtherOff && elemSizeOtherOff <= 2059L)
            ls.apply(from).sizeLemmaAnyOffset(offset, otherOffset)
            TStorage_ControlDiagnosticParameterReport_configuration.sizeLemmaAnyOffset(ls, offset + elemSizeOff, otherOffset + elemSizeOtherOff, from + 1, to)
        }
    }.ensuring { (_: Unit) => 
        TStorage_ControlDiagnosticParameterReport_configuration.sizeRange(ls, offset, from, to) == TStorage_ControlDiagnosticParameterReport_configuration.sizeRange(ls, otherOffset, from, to)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextByte(ls: Vector[TStorage_ControlDiagnosticParameterReportConfiguration], offset: Long, otherOffset: Long, from: Int, to: Int): Unit = {
        require((0 <= from) && (from <= to) && (to <= ls.size) && (ls.size <= 31))
        require((0L <= offset) && (offset <= 9223372036854775807L - 2059L * (to - from)))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854775807L - 2059L * (to - from)))
        require((offset % 8L) == (otherOffset % 8L))
        decreases(to - from)
        if (from == to) {
            ()
        } else {
            val elemSizeOff = ls.apply(from).size(offset)
            val elemSizeOtherOff = ls.apply(from).size(otherOffset)
            assert(0L <= elemSizeOff && elemSizeOff <= 2059L)
            assert(0L <= elemSizeOtherOff && elemSizeOtherOff <= 2059L)
            ls.apply(from).sizeLemmaNextByte(offset, otherOffset)
            TStorage_ControlDiagnosticParameterReport_configuration.sizeLemmaNextByte(ls, offset + elemSizeOff, otherOffset + elemSizeOtherOff, from + 1, to)
        }
    }.ensuring { (_: Unit) => 
        TStorage_ControlDiagnosticParameterReport_configuration.sizeRange(ls, offset, from, to) == TStorage_ControlDiagnosticParameterReport_configuration.sizeRange(ls, otherOffset, from, to)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextWord(ls: Vector[TStorage_ControlDiagnosticParameterReportConfiguration], offset: Long, otherOffset: Long, from: Int, to: Int): Unit = {
        require((0 <= from) && (from <= to) && (to <= ls.size) && (ls.size <= 31))
        require((0L <= offset) && (offset <= 9223372036854775807L - 2059L * (to - from)))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854775807L - 2059L * (to - from)))
        require((offset % 16L) == (otherOffset % 16L))
        decreases(to - from)
        if (from == to) {
            ()
        } else {
            val elemSizeOff = ls.apply(from).size(offset)
            val elemSizeOtherOff = ls.apply(from).size(otherOffset)
            assert(0L <= elemSizeOff && elemSizeOff <= 2059L)
            assert(0L <= elemSizeOtherOff && elemSizeOtherOff <= 2059L)
            ls.apply(from).sizeLemmaNextWord(offset, otherOffset)
            TStorage_ControlDiagnosticParameterReport_configuration.sizeLemmaNextWord(ls, offset + elemSizeOff, otherOffset + elemSizeOtherOff, from + 1, to)
        }
    }.ensuring { (_: Unit) => 
        TStorage_ControlDiagnosticParameterReport_configuration.sizeRange(ls, offset, from, to) == TStorage_ControlDiagnosticParameterReport_configuration.sizeRange(ls, otherOffset, from, to)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextDWord(ls: Vector[TStorage_ControlDiagnosticParameterReportConfiguration], offset: Long, otherOffset: Long, from: Int, to: Int): Unit = {
        require((0 <= from) && (from <= to) && (to <= ls.size) && (ls.size <= 31))
        require((0L <= offset) && (offset <= 9223372036854775807L - 2059L * (to - from)))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854775807L - 2059L * (to - from)))
        require((offset % 32L) == (otherOffset % 32L))
        decreases(to - from)
        if (from == to) {
            ()
        } else {
            val elemSizeOff = ls.apply(from).size(offset)
            val elemSizeOtherOff = ls.apply(from).size(otherOffset)
            assert(0L <= elemSizeOff && elemSizeOff <= 2059L)
            assert(0L <= elemSizeOtherOff && elemSizeOtherOff <= 2059L)
            ls.apply(from).sizeLemmaNextDWord(offset, otherOffset)
            TStorage_ControlDiagnosticParameterReport_configuration.sizeLemmaNextDWord(ls, offset + elemSizeOff, otherOffset + elemSizeOtherOff, from + 1, to)
        }
    }.ensuring { (_: Unit) => 
        TStorage_ControlDiagnosticParameterReport_configuration.sizeRange(ls, offset, from, to) == TStorage_ControlDiagnosticParameterReport_configuration.sizeRange(ls, otherOffset, from, to)
    }
}

case class TStorage_ControlDiagnosticParameterReport (
    packetStoreID: TPacketStore_ID, 
    configuration: TStorage_ControlDiagnosticParameterReport_configuration
) {
    require(33 == this.packetStoreID.length && this.packetStoreID.indexOfOrLength(UByte.fromRaw(0.toByte)) <= 32)

    def size(offset: Long): Long = {
        require((0L <= offset) && (offset <= 9223372036854711722L))
        val size_0 = (224L)
        val size_1 = 32L
        val size_2 = this.configuration.size(offset + size_0 + size_1)
        size_0 + size_1 + size_2
    }.ensuring { (res: Long) => 
        (0L <= res) && (res <= 64085L)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaAnyOffset(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854711722L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854711722L))
        val size_0_offset = (224L)
        val size_0_otherOffset = 224L
        val size_1_offset = 32L
        val size_1_otherOffset = 32L
        val size_2_offset = this.configuration.size(offset + size_0_offset + size_1_offset)
        val size_2_otherOffset = this.configuration.size(otherOffset + size_0_otherOffset + size_1_otherOffset)
        this.configuration.sizeLemmaAnyOffset(offset + size_0_offset + size_1_offset, otherOffset + size_0_otherOffset + size_1_otherOffset)
        ()
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextByte(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854711722L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854711722L))
        require((offset % 8L) == (otherOffset % 8L))
        val size_0_offset = (224L)
        val size_0_otherOffset = 224L
        val size_1_offset = 32L
        val size_1_otherOffset = 32L
        val size_2_offset = this.configuration.size(offset + size_0_offset + size_1_offset)
        val size_2_otherOffset = this.configuration.size(otherOffset + size_0_otherOffset + size_1_otherOffset)
        this.configuration.sizeLemmaNextByte(offset + size_0_offset + size_1_offset, otherOffset + size_0_otherOffset + size_1_otherOffset)
        ()
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextWord(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854711722L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854711722L))
        require((offset % 16L) == (otherOffset % 16L))
        val size_0_offset = (224L)
        val size_0_otherOffset = 224L
        val size_1_offset = 32L
        val size_1_otherOffset = 32L
        val size_2_offset = this.configuration.size(offset + size_0_offset + size_1_offset)
        val size_2_otherOffset = this.configuration.size(otherOffset + size_0_otherOffset + size_1_otherOffset)
        this.configuration.sizeLemmaNextWord(offset + size_0_offset + size_1_offset, otherOffset + size_0_otherOffset + size_1_otherOffset)
        ()
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }

    @ghost @opaque @inlineOnce
    def sizeLemmaNextDWord(offset: Long, otherOffset: Long): Unit = {
        require((0L <= offset) && (offset <= 9223372036854711722L))
        require((0L <= otherOffset) && (otherOffset <= 9223372036854711722L))
        require((offset % 32L) == (otherOffset % 32L))
        val size_0_offset = (224L)
        val size_0_otherOffset = 224L
        val size_1_offset = 32L
        val size_1_otherOffset = 32L
        val size_2_offset = this.configuration.size(offset + size_0_offset + size_1_offset)
        val size_2_otherOffset = this.configuration.size(otherOffset + size_0_otherOffset + size_1_otherOffset)
        this.configuration.sizeLemmaNextDWord(offset + size_0_offset + size_1_offset, otherOffset + size_0_otherOffset + size_1_otherOffset)
        ()
    }.ensuring { (res: Unit) => 
        this.size(offset) == this.size(otherOffset)
    }
}


@inline @cCode.inline val ERR_STORAGE_CONTROLDIAGNOSTICPARAMETERREPORT_CONFIGURATION: Int = 2695 /*(SIZE(0 .. maxConfigurationSize)) */
@inline @cCode.inline val ERR_STORAGE_CONTROLDIAGNOSTICPARAMETERREPORT_CONFIGURATION_ELM_2: Int = 2690 /* */

@inline @cCode.inline val ERR_STORAGE_CONTROLDIAGNOSTICPARAMETERREPORT: Int = 2700 /* */
@inline @cCode.inline val ERR_STORAGE_CONTROLDIAGNOSTICPARAMETERREPORT_PACKETSTOREID_2: Int = 2631 /* */

 
 

@inline @cCode.inline val ERR_ACN_ENCODE_STORAGE_CONTROLDIAGNOSTICPARAMETERREPORT: Int = 2703 /*  */
@inline @cCode.inline val ERR_ACN_ENCODE_STORAGE_CONTROLDIAGNOSTICPARAMETERREPORT_PACKETSTOREID_2: Int = 2634 /*  */
@inline @cCode.inline val ERR_ACN_ENCODE_STORAGE_CONTROLDIAGNOSTICPARAMETERREPORT_N1_UNINITIALIZED: Int = 2704 /*  */
@inline @cCode.inline val ERR_ACN_ENCODE_STORAGE_CONTROLDIAGNOSTICPARAMETERREPORT_N1: Int = 2636 /*  */
@inline @cCode.inline val ERR_ACN_ENCODE_STORAGE_CONTROLDIAGNOSTICPARAMETERREPORT_CONFIGURATION: Int = 2698 /*  */
@inline @cCode.inline val ERR_ACN_ENCODE_STORAGE_CONTROLDIAGNOSTICPARAMETERREPORT_CONFIGURATION_ELM_2: Int = 2693 /*  */
@inline @cCode.inline val TStorage_ControlDiagnosticParameterReport_REQUIRED_BYTES_FOR_ACN_ENCODING = 8011
@inline @cCode.inline val TStorage_ControlDiagnosticParameterReport_REQUIRED_BITS_FOR_ACN_ENCODING = 64085

@inline @cCode.inline val ERR_ACN_DECODE_STORAGE_CONTROLDIAGNOSTICPARAMETERREPORT: Int = 2705 /*  */
@inline @cCode.inline val ERR_ACN_DECODE_STORAGE_CONTROLDIAGNOSTICPARAMETERREPORT_PACKETSTOREID_2: Int = 2635 /*  */
@inline @cCode.inline val ERR_ACN_DECODE_STORAGE_CONTROLDIAGNOSTICPARAMETERREPORT_N1: Int = 2637 /*  */
@inline @cCode.inline val ERR_ACN_DECODE_STORAGE_CONTROLDIAGNOSTICPARAMETERREPORT_CONFIGURATION: Int = 2699 /*  */
@inline @cCode.inline val ERR_ACN_DECODE_STORAGE_CONTROLDIAGNOSTICPARAMETERREPORT_CONFIGURATION_ELM_2: Int = 2694 /*  */


