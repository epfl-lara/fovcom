/*
Code automatically generated by asn1scc tool
*/
package asn1src

import asn1scala._
import stainless.lang.{ghost => ghostExpr, _}
import stainless.annotation._
import stainless.collection._
import stainless.proof._
import StaticChecks._


def TMemory_ID_IsConstraintValid(pVal: TMemory_ID): Either[ErrorCode, Int] =
{
    var ret: Either[ErrorCode, Int] = Right(0)
    ret = (pVal == TMemory_ID.TmemId1) match
        case true =>
            Right(0)
        case false =>
            Left(45)
    ret
}

def TMemory_ID_Initialize(): TMemory_ID = TMemory_ID.TmemId1

@opaque @inlineOnce 
def TMemory_ID_ACN_Encode(pVal: TMemory_ID, codec: ACN, bCheckConstraints: Boolean): Either[ErrorCode, Int] =  // acn:21
{
    require(codec.base.bitStream.validate_offset_bits(32L))
    TMemory_ID_IsConstraintValid(pVal) match
        case Left(l) => return Left(l)
        case Right(_) =>
    @ghost val oldCdc = snapshot(codec)
    val intVal_pVal = pVal match
        case TMemory_ID.TmemId1 => ULong.fromRaw(0L)

    locally {
        ghostExpr {
            @opaque @inlineOnce
            def bitCountLemma(v: ULong): Unit = {
                require(v <= ULong.fromRaw(0))
            }.ensuring(_ => GetBitCountUnsigned(v) <= 4294967295L)
            bitCountLemma(intVal_pVal)
        }
    }
    codec.enc_Int_PositiveInteger_ConstSize_big_endian_32(intVal_pVal)
    Right(0)
}.ensuring { (res : Either[ErrorCode, Int]) =>
    res match {
        case Left(_) =>
            true
        case Right(_) =>
            old(codec).base.buf.length == codec.base.buf.length && codec.base.bitStream.bitIndex == old(codec).base.bitStream.bitIndex + 32L
    }
}

@opaque @inlineOnce 
def TMemory_ID_ACN_Decode(codec: ACN): EitherMut[ErrorCode, TMemory_ID] =
{
    require(codec.base.bitStream.validate_offset_bits(32L))

    @ghost val oldCdc = snapshot(codec)
    val intVal_pVal = codec.dec_Int_PositiveInteger_ConstSize_big_endian_32()

    val pVal = intVal_pVal.toRaw match
        case 0 => TMemory_ID.TmemId1
        case _ => return LeftMut(ERR_ACN_DECODE_MEMORY_ID)

    TMemory_ID_IsConstraintValid(pVal) match
        case Left(l) => LeftMut[ErrorCode, TMemory_ID](l)
        case Right(_) => RightMut[ErrorCode, TMemory_ID](pVal)
}.ensuring { (res : EitherMut[ErrorCode, TMemory_ID]) =>
    res match {
        case LeftMut(_) =>
            true
        case RightMut(resVal) =>
            old(codec).base.buf == codec.base.buf && codec.base.bitStream.bitIndex == old(codec).base.bitStream.bitIndex + 32L && TMemory_ID_IsConstraintValid(resVal).isRight
    }
}

@ghost @pure 
def TMemory_ID_ACN_Decode_pure(codec: ACN): (ACN, EitherMut[ErrorCode, TMemory_ID]) =
{
    require(codec.base.bitStream.validate_offset_bits(32L))
    val cpy = snapshot(codec)
    val res = TMemory_ID_ACN_Decode(cpy)
    (cpy, res)
}